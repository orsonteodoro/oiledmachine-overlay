diff '--color=auto' -urp blender-5.2.0.orig/build_files/cmake/macros.cmake blender-5.2.0/build_files/cmake/macros.cmake
--- blender-5.2.0.orig/build_files/cmake/macros.cmake	2026-07-13 08:20:11.000000000 -0700
+++ blender-5.2.0/build_files/cmake/macros.cmake	2026-08-22 03:59:30.166487157 -0700
@@ -964,6 +964,7 @@ function(get_blender_version)
   # - BLENDER_VERSION_MINOR
   # - BLENDER_VERSION_PATCH
   # - BLENDER_VERSION_CYCLE (alpha, beta, rc, release)
+  # - BLENDER_VERSION_TAG (_filmic) to indicate type of mod
 
   # So CMAKE depends on `BKE_blender_version.h`, beware of infinite-loops!
   configure_file(
@@ -988,6 +989,10 @@ function(get_blender_version)
     REGEX REPLACE ".*#define[ \t]+BLENDER_VERSION_CYCLE[ \t]+([a-z]+).*" "\\1"
     _out_version_cycle "${_contents}"
   )
+  string(
+    REGEX REPLACE ".*#define[ \t]+BLENDER_VERSION_TAG[ \t]+([a-z_]*).*" "\\1"
+    _out_version_tag "${_contents}"
+  )
 
   if(NOT ${_out_version} MATCHES "^[0-9]+$")
     message(FATAL_ERROR "Version parsing failed for BLENDER_VERSION")
@@ -1001,6 +1006,10 @@ function(get_blender_version)
     message(FATAL_ERROR "Version parsing failed for BLENDER_VERSION_CYCLE")
   endif()
 
+  #if(NOT ${_out_version_tag} MATCHES "[a-z_]+")
+  #	message(FATAL_ERROR "Version parsing failed for BLENDER_VERSION_TAG")
+  #endif()
+
   math(EXPR _out_version_major "${_out_version} / 100")
   math(EXPR _out_version_minor "${_out_version} % 100")
 
@@ -1010,6 +1019,7 @@ function(get_blender_version)
   set(BLENDER_VERSION_MINOR "${_out_version_minor}" PARENT_SCOPE)
   set(BLENDER_VERSION_PATCH "${_out_version_patch}" PARENT_SCOPE)
   set(BLENDER_VERSION_CYCLE "${_out_version_cycle}" PARENT_SCOPE)
+  set(BLENDER_VERSION_TAG "${_out_version_tag}" PARENT_SCOPE)
 
 endfunction()
 
Only in blender-5.2.0/build_files/cmake: macros.cmake.orig
diff '--color=auto' -urp blender-5.2.0.orig/source/blender/blenkernel/BKE_blender_version.h blender-5.2.0/source/blender/blenkernel/BKE_blender_version.h
--- blender-5.2.0.orig/source/blender/blenkernel/BKE_blender_version.h	2026-07-13 08:20:11.000000000 -0700
+++ blender-5.2.0/source/blender/blenkernel/BKE_blender_version.h	2026-08-22 04:00:02.930872460 -0700
@@ -27,6 +27,8 @@ namespace blender {
 #define BLENDER_VERSION_CYCLE release
 /** Blender release type suffix. LTS or blank. */
 #define BLENDER_VERSION_SUFFIX LTS
+/* tag or flavor */
+#define BLENDER_VERSION_TAG   
 
 /* Blender file format version. */
 #define BLENDER_FILE_VERSION BLENDER_VERSION
Only in blender-5.2.0/source/blender/blenkernel: BKE_blender_version.h.orig
Only in blender-5.2.0/source/blender/blenkernel: BKE_blender_version.h.rej
diff '--color=auto' -urp blender-5.2.0.orig/source/blender/blenkernel/intern/appdir.cc blender-5.2.0/source/blender/blenkernel/intern/appdir.cc
--- blender-5.2.0.orig/source/blender/blenkernel/intern/appdir.cc	2026-07-13 08:20:11.000000000 -0700
+++ blender-5.2.0/source/blender/blenkernel/intern/appdir.cc	2026-08-22 03:59:30.167345731 -0700
@@ -124,9 +124,9 @@ void BKE_appdir_exit()
  */
 static char *blender_version_decimal(const int version)
 {
-  static char version_str[5];
+  static char version_str[8];
   BLI_assert(version < 1000);
-  SNPRINTF(version_str, "%d.%d", version / 100, version % 100);
+  SNPRINTF(version_str, "%d.%d.%s%s", version / 100, version % 100, STRINGIFY(BLENDER_VERSION_PATCH), STRINGIFY(BLENDER_VERSION_TAG));
   return version_str;
 }
 
Only in blender-5.2.0/source/blender/blenkernel/intern: appdir.cc.orig
diff '--color=auto' -urp blender-5.2.0.orig/source/creator/CMakeLists.txt blender-5.2.0/source/creator/CMakeLists.txt
--- blender-5.2.0.orig/source/creator/CMakeLists.txt	2026-08-22 03:59:11.932205271 -0700
+++ blender-5.2.0/source/creator/CMakeLists.txt	2026-08-22 03:59:30.167907662 -0700
@@ -383,20 +383,20 @@ if(UNIX AND NOT APPLE)
   if(WITH_PYTHON_MODULE)
     if(WITH_INSTALL_PORTABLE)
       set(TARGETDIR_BPY "bpy")
-      set(TARGETDIR_VER "bpy/${BLENDER_VERSION}")
+      set(TARGETDIR_VER "bpy/${BLENDER_VERSION}.${BLENDER_VERSION_PATCH}${BLENDER_VERSION_TAG}")
       set(TARGETDIR_LIB "bpy/lib")
     else()
       set(TARGETDIR_BPY ${PYTHON_SITE_PACKAGES}/bpy)
-      set(TARGETDIR_VER ${PYTHON_SITE_PACKAGES}/bpy/${BLENDER_VERSION})
+      set(TARGETDIR_VER ${PYTHON_SITE_PACKAGES}/bpy/${BLENDER_VERSION}.${BLENDER_VERSION_PATCH}${BLENDER_VERSION_TAG})
       set(TARGETDIR_LIB ${PYTHON_SITE_PACKAGES}/bpy/lib)
     endif()
   else()
     if(WITH_INSTALL_PORTABLE)
-      set(TARGETDIR_VER "${BLENDER_VERSION}")
+      set(TARGETDIR_VER "${BLENDER_VERSION}.${BLENDER_VERSION_PATCH}${BLENDER_VERSION_TAG}")
       set(TARGETDIR_TEXT ".")
       set(TARGETDIR_LIB "lib")
     else()
-      set(TARGETDIR_VER "share/blender/${BLENDER_VERSION}")
+      set(TARGETDIR_VER "share/blender/${BLENDER_VERSION}.${BLENDER_VERSION_PATCH}${BLENDER_VERSION_TAG}")
       set(TARGETDIR_TEXT "share/doc/blender")
       if(CMAKE_INSTALL_LIBDIR)
         set(TARGETDIR_VER_LIBS "${CMAKE_INSTALL_LIBDIR}/blender/${BLENDER_VERSION}")
@@ -1286,7 +1286,7 @@ elseif(WIN32)
 
       install(
         DIRECTORY ${LIBDIR}/python/${_PYTHON_VERSION_NO_DOTS}/lib
-        DESTINATION ${BLENDER_VERSION}/python/
+        DESTINATION ${BLENDER_VERSION}.${BLENDER_VERSION_PATCH}${BLENDER_VERSION_TAG}/python/
         CONFIGURATIONS Release;RelWithDebInfo;MinSizeRel
         PATTERN "*_d.*" EXCLUDE                 # * debug libraries *
         PATTERN "__pycache__" EXCLUDE           # * any cache *
@@ -1296,7 +1296,7 @@ elseif(WIN32)
 
       install(
         DIRECTORY ${LIBDIR}/python/${_PYTHON_VERSION_NO_DOTS}/lib
-        DESTINATION ${BLENDER_VERSION}/python/
+        DESTINATION ${BLENDER_VERSION}.${BLENDER_VERSION_PATCH}${BLENDER_VERSION_TAG}/python/
         CONFIGURATIONS Debug
         PATTERN "__pycache__" EXCLUDE           # * any cache *
         PATTERN "*.pyc" EXCLUDE                 # * any cache *
@@ -1305,7 +1305,7 @@ elseif(WIN32)
 
       install(
         DIRECTORY ${LIBDIR}/python/${_PYTHON_VERSION_NO_DOTS}/DLLs
-        DESTINATION ${BLENDER_VERSION}/python
+        DESTINATION ${BLENDER_VERSION}.${BLENDER_VERSION_PATCH}${BLENDER_VERSION_TAG}/python
         CONFIGURATIONS Release;RelWithDebInfo;MinSizeRel
         PATTERN "*.pdb" EXCLUDE
         PATTERN "*_d.*" EXCLUDE
@@ -1313,7 +1313,7 @@ elseif(WIN32)
 
       install(
         DIRECTORY ${LIBDIR}/python/${_PYTHON_VERSION_NO_DOTS}/DLLs
-        DESTINATION ${BLENDER_VERSION}/python
+        DESTINATION ${BLENDER_VERSION}.${BLENDER_VERSION_PATCH}${BLENDER_VERSION_TAG}/python
         CONFIGURATIONS Debug
       )
 
Only in blender-5.2.0/source/creator: CMakeLists.txt.orig
