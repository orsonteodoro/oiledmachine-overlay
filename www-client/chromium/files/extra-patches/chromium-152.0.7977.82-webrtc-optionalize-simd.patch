--- a/third_party/webrtc/common_audio/BUILD.gn.orig	2026-09-02 13:40:03.000000000 -0700
+++ b/third_party/webrtc/common_audio/BUILD.gn	2026-09-08 07:58:54.163568163 -0700
@@ -6,6 +6,7 @@
 # in the file PATENTS.  All contributing project authors may
 # be found in the AUTHORS file in the root of the source tree.
 
+import("//build/config/simd.gni")
 import("../webrtc.gni")
 
 visibility = [ ":*" ]
@@ -67,8 +68,12 @@ rtc_library("common_audio") {
   }
 
   if (current_cpu == "x86" || current_cpu == "x64") {
-    deps += [ ":common_audio_sse2" ]
-    deps += [ ":common_audio_avx2" ]
+    if (use_sse2) {
+      deps += [ ":common_audio_sse2" ]
+    }
+    if (use_avx2 && use_fma) {
+      deps += [ ":common_audio_avx2" ]
+    }
   }
 }
 
@@ -228,8 +233,12 @@ rtc_library("fir_filter_factory") {
     "../rtc_base/system:arch",
   ]
   if (current_cpu == "x86" || current_cpu == "x64") {
-    deps += [ ":common_audio_sse2" ]
-    deps += [ ":common_audio_avx2" ]
+    if (use_sse2) {
+      deps += [ ":common_audio_sse2" ]
+    }
+    if (use_avx2 && use_fma) {
+      deps += [ ":common_audio_avx2" ]
+    }
   }
   if (rtc_build_with_neon) {
     deps += [ ":common_audio_neon" ]
--- a/third_party/webrtc/modules/audio_processing/agc2/rnn_vad/BUILD.gn.orig	2026-09-02 13:40:03.000000000 -0700
+++ b/third_party/webrtc/modules/audio_processing/agc2/rnn_vad/BUILD.gn	2026-09-08 07:58:54.163928919 -0700
@@ -6,6 +6,7 @@
 # in the file PATENTS.  All contributing project authors may
 # be found in the AUTHORS file in the root of the source tree.
 
+import("//build/config/simd.gni")
 import("../../../../webrtc.gni")
 
 rtc_library("rnn_vad") {
@@ -97,7 +98,9 @@ rtc_library("rnn_vad_layers") {
     "//third_party/rnnoise:rnn_vad",
   ]
   if (current_cpu == "x86" || current_cpu == "x64") {
-    deps += [ ":vector_math_avx2" ]
+    if (use_avx2 && use_fma) {
+      deps += [ ":vector_math_avx2" ]
+    }
   }
 }
 
@@ -156,7 +159,9 @@ rtc_library("rnn_vad_pitch") {
     "../../../../rtc_base/system:arch",
   ]
   if (current_cpu == "x86" || current_cpu == "x64") {
-    deps += [ ":vector_math_avx2" ]
+    if (use_avx2 && use_fma) {
+      deps += [ ":vector_math_avx2" ]
+    }
   }
 }
 
--- a/third_party/webrtc/modules/audio_processing/aec3/BUILD.gn.orig	2026-09-02 13:40:03.000000000 -0700
+++ b/third_party/webrtc/modules/audio_processing/aec3/BUILD.gn	2026-09-08 07:58:54.164212603 -0700
@@ -6,6 +6,7 @@
 # in the file PATENTS.  All contributing project authors may
 # be found in the AUTHORS file in the root of the source tree.
 
+import("//build/config/simd.gni")
 import("../../../webrtc.gni")
 if (rtc_enable_protobuf) {
   import("//third_party/protobuf/proto_library.gni")
@@ -164,7 +165,9 @@ rtc_library("aec3") {
   ]
 
   if (current_cpu == "x86" || current_cpu == "x64") {
-    deps += [ ":aec3_avx2" ]
+    if (use_avx2 && use_fma) {
+      deps += [ ":aec3_avx2" ]
+    }
   }
 }
 
--- a/third_party/webrtc/common_audio/third_party/ooura/BUILD.gn.orig	2026-09-02 13:40:03.000000000 -0700
+++ b/third_party/webrtc/common_audio/third_party/ooura/BUILD.gn	2026-09-08 07:58:54.164469052 -0700
@@ -6,6 +6,7 @@
 # in the file PATENTS.  All contributing project authors may
 # be found in the AUTHORS file in the root of the source tree.
 
+import("//build/config/simd.gni")
 import("../../../webrtc.gni")
 
 rtc_library("fft_size_128") {
@@ -21,12 +22,14 @@ rtc_library("fft_size_128") {
   cflags = []
 
   if (current_cpu == "x86" || current_cpu == "x64") {
-    sources += [
-      "fft_size_128/ooura_fft_sse2.cc",
-      "fft_size_128/ooura_fft_tables_neon_sse2.h",
-    ]
-    if (is_posix || is_fuchsia) {
-      cflags += [ "-msse2" ]
+    if (use_sse2) {
+      sources += [
+        "fft_size_128/ooura_fft_sse2.cc",
+        "fft_size_128/ooura_fft_tables_neon_sse2.h",
+      ]
+      if (is_posix || is_fuchsia) {
+        cflags += [ "-msse2" ]
+      }
     }
   }
 
