--- a/build/config/compiler_cpu_abi.gn.orig	2026-06-23 16:37:18.000000000 -0700
+++ b/build/config/compiler_cpu_abi.gn	2026-07-02 10:31:42.141763859 -0700
@@ -4,6 +4,7 @@
 
 import("//build/config/chromeos/args.gni")
 import("//build/toolchain/toolchain.gni")
+import("//build/config/simd.gni")
 
 if (current_cpu == "arm" || current_cpu == "arm64") {
   import("//build/config/arm.gni")
@@ -55,7 +56,9 @@ if ((is_posix && !is_apple) || is_fuchsi
       cpu_abi_cflags += [ "-m64" ]
       cpu_abi_ldflags += [ "-m64" ]
     }
-    cpu_abi_cflags += [ "-msse3" ]
+    if (use_sse3) {
+      cpu_abi_cflags += [ "-msse3" ]
+    }
     if (target_os == "chromeos" && cros_target_cpu_arch != "" &&
         is_a_target_toolchain) {
       cpu_abi_cflags += [ "-march=$cros_target_cpu_arch" ]
@@ -69,10 +72,12 @@ if ((is_posix && !is_apple) || is_fuchsi
       cpu_abi_cflags += [ "-m32" ]
       cpu_abi_ldflags += [ "-m32" ]
     }
-    cpu_abi_cflags += [
-      "-mfpmath=sse",
-      "-msse3",
-    ]
+    if (use_sse3) {
+      cpu_abi_cflags += [
+        "-mfpmath=sse",
+        "-msse3",
+      ]
+    }
   } else if (current_cpu == "arm") {
     if (is_clang && !is_android && !is_chromeos_device) {
       cpu_abi_cflags += [ "--target=arm-linux-gnueabihf" ]
@@ -171,9 +176,13 @@ if ((is_posix && !is_apple) || is_fuchsi
     }
 
     if (mips_dsp_rev == 1) {
-      cpu_abi_cflags += [ "-mdsp" ]
+      if (use_dsp) {
+        cpu_abi_cflags += [ "-mdsp" ]
+      }
     } else if (mips_dsp_rev == 2) {
-      cpu_abi_cflags += [ "-mdspr2" ]
+      if (use_dspr2) {
+        cpu_abi_cflags += [ "-mdspr2" ]
+      }
     }
 
     cpu_abi_cflags += [ "-m${mips_float_abi}-float" ]
