--- a/third_party/perfetto/src/tracing/service/tracing_service_impl.cc.orig	2026-03-15 00:18:31.848206362 -0700
+++ b/third_party/perfetto/src/tracing/service/tracing_service_impl.cc	2026-03-15 00:18:45.959897382 -0700
@@ -205,6 +205,12 @@ ssize_t writev(int fd, const struct iove
 
 #elif PERFETTO_BUILDFLAG(PERFETTO_OS_QNX)
 #define IOV_MAX 1024  // Linux compatible limit.
+#else
+  // Linux / Android / macOS / FreeBSD – pull in the system definition
+  #include <sys/uio.h>
+  #ifndef IOV_MAX
+    #define IOV_MAX 1024  // fallback
+  #endif
 #endif
 
 void SerializeAndAppendPacket(std::vector<TracePacket>* packets,
