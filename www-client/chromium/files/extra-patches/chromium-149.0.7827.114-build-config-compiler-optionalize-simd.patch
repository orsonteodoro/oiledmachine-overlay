--- a/build/config/compiler/BUILD.gn.orig	2026-06-12 17:40:04.233124724 -0700
+++ b/build/config/compiler/BUILD.gn	2026-06-12 17:44:34.264889933 -0700
@@ -18,6 +18,7 @@ import("//build/config/gclient_args.gni"
 import("//build/config/host_byteorder.gni")
 import("//build/config/pch.gni")
 import("//build/config/rust.gni")
+import("//build/config/simd.gni")
 import("//build/config/sanitizers/sanitizers.gni")
 import("//build/config/ui.gni")
 import("//build/config/unwind.gni")
@@ -1357,7 +1358,9 @@ config("compiler_cpu_abi") {
         cflags += [ "-m64" ]
         ldflags += [ "-m64" ]
       }
-      cflags += [ "-msse3" ]
+      if (use_sse3) {
+        cflags += [ "-msse3" ]
+      }
       if (target_os == "chromeos" && cros_target_cpu_arch != "" &&
           is_a_target_toolchain) {
         cflags += [ "-march=$cros_target_cpu_arch" ]
@@ -1371,10 +1374,12 @@ config("compiler_cpu_abi") {
         cflags += [ "-m32" ]
         ldflags += [ "-m32" ]
       }
-      cflags += [
-        "-mfpmath=sse",
-        "-msse3",
-      ]
+      if (use_sse3) {
+        cflags += [
+          "-mfpmath=sse",
+          "-msse3",
+        ]
+      }
     } else if (current_cpu == "arm") {
       if (is_clang && !is_android && !is_chromeos_device) {
         cflags += [ "--target=arm-linux-gnueabihf" ]
@@ -1473,9 +1478,13 @@ config("compiler_cpu_abi") {
       }
 
       if (mips_dsp_rev == 1) {
-        cflags += [ "-mdsp" ]
+        if (use_dsp) {
+          cflags += [ "-mdsp" ]
+        }
       } else if (mips_dsp_rev == 2) {
-        cflags += [ "-mdspr2" ]
+        if (use_dspr2) {
+          cflags += [ "-mdspr2" ]
+        }
       }
 
       cflags += [ "-m${mips_float_abi}-float" ]
