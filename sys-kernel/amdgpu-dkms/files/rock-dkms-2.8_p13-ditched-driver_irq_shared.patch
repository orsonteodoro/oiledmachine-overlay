--- a/amd/amdgpu/amdgpu_drv.c.orig	2019-10-03 02:57:50.280180187 -0700
+++ b/amd/amdgpu/amdgpu_drv.c	2019-10-03 03:15:22.856608497 -0700
@@ -1469,7 +1469,11 @@ static struct drm_driver kms_driver = {
 #if DRM_VERSION_CODE >= DRM_VERSION(4, 20, 0)
 		DRIVER_ATOMIC |
 #endif
+#if DRM_VERSION_CODE < DRM_VERSION(5, 1, 0)
 		DRIVER_HAVE_IRQ | DRIVER_IRQ_SHARED | DRIVER_GEM |
+#else
+		DRIVER_GEM |
+#endif
 #if defined(HAVE_CHUNK_ID_SYNOBJ_IN_OUT)
 		DRIVER_SYNCOBJ |
 #endif
