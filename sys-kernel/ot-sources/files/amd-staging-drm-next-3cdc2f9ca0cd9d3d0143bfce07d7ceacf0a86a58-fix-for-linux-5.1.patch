--- a/drivers/gpu/drm/v3d/v3d_sched.c.orig	2019-05-05 17:42:58.000000000 -0700
+++ b/drivers/gpu/drm/v3d/v3d_sched.c	2019-05-07 19:32:30.014346422 -0700
@@ -234,7 +234,7 @@ v3d_gpu_reset_for_timeout(struct v3d_dev
 	for (q = 0; q < V3D_MAX_QUEUES; q++) {
 		struct drm_gpu_scheduler *sched = &v3d->queue[q].sched;
 
-		drm_sched_stop(sched);
+		drm_sched_stop(sched, sched_job);
 
 		if(sched_job)
 			drm_sched_increase_karma(sched_job);
