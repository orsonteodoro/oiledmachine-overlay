There is change in

https://github.com/torvalds/linux/blob/master/arch/powerpc/kernel/prom_init.c#L2438

which I cannot find the commit summary and reason why this hunk exists.

--- a/drivers/video/logo/Kconfig.orig	2026-04-12 13:48:06.000000000 -0700
+++ b/drivers/video/logo/Kconfig	2026-04-24 15:43:14.838687909 -0700
@@ -76,4 +76,16 @@ config LOGO_LINUX_CLUT224_FILE
 
 	    magick source_image -compress none -colors 224 destination.ppm
 
+config LOGO_CUSTOM_MONO
+	bool "Custom logo (black and white)"
+	default y
+
+config LOGO_CUSTOM_VGA16
+	bool "Custom logo (16 colors)"
+	default y
+
+config LOGO_CUSTOM_CLUT224
+	bool "Custom logo (224 colors)"
+	default y
+
 endif # LOGO
--- a/drivers/video/logo/Makefile.orig	2026-04-12 13:48:06.000000000 -0700
+++ b/drivers/video/logo/Makefile	2026-04-24 15:43:34.394611959 -0700
@@ -5,6 +5,9 @@ obj-$(CONFIG_LOGO)			+= logo.o
 obj-$(CONFIG_LOGO_LINUX_MONO)		+= logo_linux_mono.o
 obj-$(CONFIG_LOGO_LINUX_VGA16)		+= logo_linux_vga16.o
 obj-$(CONFIG_LOGO_LINUX_CLUT224)	+= logo_linux_clut224.o
+obj-$(CONFIG_LOGO_CUSTOM_MONO)		+= logo_custom_mono.o
+obj-$(CONFIG_LOGO_CUSTOM_VGA16)		+= logo_custom_vga16.o
+obj-$(CONFIG_LOGO_CUSTOM_CLUT224)	+= logo_custom_clut224.o
 
 obj-$(CONFIG_SPU_BASE)			+= logo_spe_clut224.o
 
--- a/drivers/video/logo/logo.c.orig	2026-04-12 13:48:06.000000000 -0700
+++ b/drivers/video/logo/logo.c	2026-04-24 15:46:17.637693759 -0700
@@ -48,20 +48,35 @@ const struct linux_logo * __ref fb_find_
 	if (nologo || logos_freed)
 		return NULL;
 
+	if (depth >= 1) {
 #ifdef CONFIG_LOGO_LINUX_MONO
-	if (depth >= 1)
 		logo = &logo_linux_mono;
 #endif
+#ifdef CONFIG_LOGO_CUSTOM_MONO
+		/* Custom logo */
+		logo = &logo_custom_mono;
+#endif
+	}
 	
+	if (depth >= 4) {
 #ifdef CONFIG_LOGO_LINUX_VGA16
-	if (depth >= 4)
 		logo = &logo_linux_vga16;
 #endif
+#ifdef CONFIG_LOGO_CUSTOM_VGA16
+		/* Custom logo */
+		logo = &logo_custom_vga16;
+#endif
+	}
 	
+	if (depth >= 8) {
 #ifdef CONFIG_LOGO_LINUX_CLUT224
-	if (depth >= 8)
 		logo = &logo_linux_clut224;
 #endif
+#ifdef CONFIG_LOGO_CUSTOM_CLUT224
+		/* Custom logo */
+		logo = &logo_custom_clut224;
+#endif
+	}
 
 	return logo;
 }
--- a/include/linux/linux_logo.h.orig	2026-04-12 13:48:06.000000000 -0700
+++ b/include/linux/linux_logo.h	2026-04-24 15:43:14.839433122 -0700
@@ -34,6 +34,9 @@ extern const struct linux_logo logo_linu
 extern const struct linux_logo logo_linux_vga16;
 extern const struct linux_logo logo_linux_clut224;
 extern const struct linux_logo logo_spe_clut224;
+extern const struct linux_logo logo_custom_mono;
+extern const struct linux_logo logo_custom_vga16;
+extern const struct linux_logo logo_custom_clut224;
 
 extern const struct linux_logo *fb_find_logo(int depth);
 #ifdef CONFIG_FB_LOGO_EXTRA
--- a/arch/powerpc/kernel/prom_init_check.sh.orig	2026-04-12 13:48:06.000000000 -0700
+++ b/arch/powerpc/kernel/prom_init_check.sh	2026-04-24 15:43:14.840203616 -0700
@@ -29,7 +29,7 @@ fi
 WHITELIST="add_reloc_offset __bss_start __bss_stop copy_and_flush
 _end enter_prom $MEM_FUNCS reloc_offset __secondary_hold
 __secondary_hold_acknowledge __secondary_hold_spinloop __start
-logo_linux_clut224 btext_prepare_BAT
+logo_linux_clut224 logo_custom_clut224 btext_prepare_BAT
 reloc_got2 kernstart_addr memstart_addr linux_banner _stext
 btext_setup_display TOC. relocate"
 
--- a/arch/powerpc/kernel/prom_init.c.orig	2026-04-12 13:48:06.000000000 -0700
+++ b/arch/powerpc/kernel/prom_init.c	2026-04-24 15:43:14.840619143 -0700
@@ -2450,6 +2450,13 @@ static void __init prom_check_displays(v
 					   clut[2]) != 0)
 				break;
 #endif /* CONFIG_LOGO_LINUX_CLUT224 */
+#ifdef CONFIG_LOGO_CUSTOM_CLUT224
+		clut = PTRRELOC(logo_custom_clut224.clut);
+		for (i = 0; i < logo_custom_clut224.clutsize; i++, clut += 3)
+			if (prom_set_color(ih, i + 32, clut[0], clut[1],
+					   clut[2]) != 0)
+				break;
+#endif /* CONFIG_LOGO_CUSTOM_CLUT224 */
 
 #ifdef CONFIG_PPC_EARLY_DEBUG_BOOTX
 		if (prom_getprop(node, "linux,boot-display", NULL, 0) !=
