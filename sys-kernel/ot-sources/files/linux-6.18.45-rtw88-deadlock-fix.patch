This is an AI generated patch to fix issue with deadlock and forced reset to fix
network.

This will restore the 30 day uptime instead of doing a hard reset every day.

The low uptime issue happened around CopyFail and Fragnesia fix was submitted.

At around the time a deadlock happens, the kernel will output to dmesg the
following:

[73768.919526] wlp0s18f2u4: Limiting TX power to 127 (127 - 0) dBm as advertised by <redacted>
[74893.329552] rtw88_8821cu 2-4:1.0: read register 0xaa failed with -110
[74893.869270] rtw88_8821cu 2-4:1.0: rtw_usb_reg_sec: reg 0x4e0, usb write 1 fail, status: -110
[74894.926330] rtw88_8821cu 2-4:1.0: read register 0x770 failed with -110
[74895.982336] rtw88_8821cu 2-4:1.0: read register 0x774 failed with -110
[74896.526379] rtw88_8821cu 2-4:1.0: write register 0x76e failed with -110
[74897.582483] rtw88_8821cu 2-4:1.0: read register 0x1cc failed with -110
[74899.186462] rtw88_8821cu 2-4:1.0: write register 0x1f0 failed with -110
[74899.730467] rtw88_8821cu 2-4:1.0: write register 0x1d0 failed with -110
[74909.778768] rtw88_8821cu 2-4:1.0: write register 0x9a4 failed with -110
[74990.768155] rtw88_8821cu 2-4:1.0: rtw_usb_reg_sec: reg 0x4e0, usb write 1 fail, status: -110
[74992.369198] rtw88_8821cu 2-4:1.0: rtw_usb_reg_sec: reg 0x4e0, usb write 1 fail, status: -110

When analyzing commit 6b96494, I noticed that there was a freed memory but no
mutex unlock and the code reviewer mindlessly okaying the commit into master.

Code reviewers should consider the mutex unlocking whenever there is a memory
free before rashly approving the commit.

The driver has been broken for more than 3 months.

When the deadlock happens, all network consuming commands (eg. ping, nmtui)
will deadlock.  Also, it is not possible to unload the driver while
deadlocking.  nmtui will refuse to deactivate or rescan for Wi-Fi.
It causes OpenRC to halt before restart because the init script cannot
unload the driver.  It may cause compiler to halt because it affects maybe
the IO scheduler.  So I am forced to reset the computer.  I tried unsetting
CONFIG_CFG80211_DEFAULT_PS and setting CONFIG_USB_AUTOSUSPEND_DELAY=-1 for
the kernel config, and the following for /etc/modprobe.d/rtw88.conf:

options rtw88_core disable_lps_deep=Y support_bf=Y
options rtw88_pci disable_aspm=Y
options rtw88_usb switch_usb_mode=N
options usbcore autosuspend=-1

and it didn't fix the deadlock and frequent disconnect issue before this patch.

For the frequent disconnect because of the recent driver security fixes, it
will cause the DNS to mess up.  Some domains will work and others not.  I have
to disconnect and reconnect more than usual after uploading.  Since applying
this AI generated patch, I am doing fewer manual resets and no forced resets
after uploading through nmtui.

Patch status:  testing
Latest test result:  issue persists
Current test result:  actively testing

AI summary of fixes [modified by ebuild maintainer]:

From: Orson Teodoro
Subject: [PATCH] rtw88: fix 30+ day -110 storm, NULL deref in restart_work and 127dBm wedge
rtw88_8821cu 2-5:1.0 fails after 10-30 days uptime with:
  read register 0xaa failed with -110
  rtw_usb_reg_sec: reg 0x4e0, usb write 1 fail, status: -110
  read register 0x770 failed with -110
Followed by 70s spam and then deadlock on modprobe -rf and
google-chrome hang with:
  firmware crash, start reset and recover
  WARNING: at __flush_workqueue
  BUG: kernel NULL pointer dereference, address: 0000000000000000
  __flush_workqueue+0x359
  ieee80211_restart_work+0x1b
  note: kworker/0:2 exited with irqs disabled
Root causes:
1) mac80211: AP <redacted> advertises country IE max power
   127 dBm (127 - 0). 127 is invalid (means no limit). Programming
   it wedges RF, leading to 0x770 -110.
2) rtw88 usb: -110 only handled in TX path, not in rtw_usb_read,
   rtw_usb_write, rtw_usb_reg_sec. Device keeps hammering 0x4e0
   after failure, fail_count never checked, causing storm.
3) rtw_usb_tx_agg_skb: ieee80211_purge_tx_queue called while
   holding usb_lock / with fw_recovery holding mutex, causing
   modprobe -rf EBUSY.
4) rtw_fw_recovery_work: Calling usb_reset_device() from inside
   fw_recovery_work that then calls ieee80211_restart_hw() on a
   device that has just disconnected (USB disconnect, device number
   2 -> new device 3). usb_reset fails with descriptor read -110 x4,
   device not accepting address, then restart_work self-flushes its
   own workqueue -> WARN + NULL deref + irqs disabled -> system
   deadlock seen as google-chrome hang.
Fix:
* mac80211: Ignore chan_pwr == 127 or >30 in
  ieee80211_handle_pwr_constr, ratelimited log.
* rtw88 usb: Add rtwdev->fail + fail_count, add rtw_usb_mark_fail(),
  check fail in all control paths (read/write/reg_sec/read32/
  write32/write_data/tx_agg), stop IO after 3x -110.
* rtw88 usb: Fix tx_agg to splice tx_ack_queue before purge outside lock.
* rtw88 core: In fw_recovery_work, do usb_reset_device outside mutex,
  check ret == -ENODEV, and abort ieee80211_restart_hw if device gone
  after reset. Prevents self-flush deadlock and chrome hang. Reset
  re-enumeration as 1-5 is expected after failed reset (EHCI->OHCI
  fallback).

--- a/drivers/net/wireless/realtek/rtw88/main.h.orig	2026-08-22 16:53:49.337325979 -0700
+++ b/drivers/net/wireless/realtek/rtw88/main.h	2026-08-22 17:02:26.564052153 -0700
@@ -14,6 +14,7 @@
 #include <linux/iopoll.h>
 #include <linux/interrupt.h>
 #include <linux/workqueue.h>
+#include <linux/atomic.h>
 
 #include "util.h"
 
@@ -2073,6 +2074,10 @@ struct rtw_dev {
 	/* ensures exclusive access from mac80211 callbacks */
 	struct mutex mutex;
 
+	/* USB fail detection for -110 storm */
+	bool fail; /* true after 3 consecutive -110 to stop USB spam */
+	atomic_t fail_count;
+
 	/* watch dog every 2 sec */
 	struct delayed_work watch_dog_work;
 	u32 watch_dog_cnt;
@@ -2262,6 +2267,7 @@ int rtw_sta_add(struct rtw_dev *rtwdev,
 void rtw_sta_remove(struct rtw_dev *rtwdev, struct ieee80211_sta *sta,
 		    bool fw_exist);
 void rtw_fw_recovery(struct rtw_dev *rtwdev);
+void rtw_fw_recovery_work(struct work_struct *work);
 int rtw_wait_firmware_completion(struct rtw_dev *rtwdev);
 int rtw_power_on(struct rtw_dev *rtwdev);
 void rtw_core_fw_scan_notify(struct rtw_dev *rtwdev, bool start);
--- a/drivers/net/wireless/realtek/rtw88/main.c.orig	2026-08-22 16:53:49.336833081 -0700
+++ b/drivers/net/wireless/realtek/rtw88/main.c	2026-08-22 17:02:58.768455460 -0700
@@ -3,6 +3,7 @@
  */
 
 #include <linux/devcoredump.h>
+#include <linux/usb.h>
 
 #include "main.h"
 #include "regd.h"
@@ -20,6 +21,7 @@
 #include "sar.h"
 #include "sdio.h"
 #include "led.h"
+#include "usb.h"
 
 bool rtw_disable_lps_deep_mode;
 EXPORT_SYMBOL(rtw_disable_lps_deep_mode);
@@ -645,6 +647,12 @@ static void __fw_recovery_work(struct rt
 	set_bit(RTW_FLAG_RESTARTING, rtwdev->flags);
 	clear_bit(RTW_FLAG_RESTART_TRIGGERING, rtwdev->flags);
 
+	/* NEW: skip fw crash dump if we are in USB -110 storm */
+	if (rtwdev->fail) {
+		rtw_warn(rtwdev, "bypass fw dump, USB failed, reset\n");
+		goto reset;
+	}
+
 	ret = rtw_fwcd_prep(rtwdev);
 	if (ret)
 		goto free;
@@ -662,6 +670,7 @@ free:
 
 	WARN(1, "firmware crash, start reset and recover\n");
 
+reset:
 	rcu_read_lock();
 	rtw_iterate_keys_rcu(rtwdev, NULL, rtw_reset_key_iter, rtwdev);
 	rcu_read_unlock();
@@ -669,18 +678,60 @@ free:
 	rtw_iterate_vifs_atomic(rtwdev, rtw_reset_vif_iter, rtwdev);
 	bitmap_zero(rtwdev->hw_port, RTW_PORT_NUM);
 	rtw_enter_ips(rtwdev);
+
+	/* Clear USB fail state after reset path prepared.
+	 * This is called with rtwdev->mutex held by caller
+	 * rtw_fw_recovery_work(), so safe to clear here.
+	 * Also clear fail_count so next -110 storm is detected anew.
+	 */
+	rtwdev->fail = false;
+	atomic_set(&rtwdev->fail_count, 0);
 }
 
 static void rtw_fw_recovery_work(struct work_struct *work)
 {
 	struct rtw_dev *rtwdev = container_of(work, struct rtw_dev,
 					      fw_recovery_work);
+	bool was_fail = rtwdev->fail;
+	struct rtw_usb *rtwusb;
+	int ret;
+
+	if (was_fail) {
+		rtwusb = rtw_get_usb_priv(rtwdev);
+		rtw_warn(rtwdev, "USB reset after -110 storm (reg 0x4e0/0xaa/0x770)\n");
+
+		/* Don't hold mutex across reset - it sleeps and
+		 * triggers disconnect. If reset fails with -ENODEV,
+		 * device is gone, don't restart_hw.
+		 */
+		ret = usb_reset_device(rtwusb->udev);
+		msleep(200);
+
+		/* If device disconnected during reset, bail out.
+		 * Let USB core re-enumerate as new device 1-5.
+		 * This is what you saw: 2-5 -> 1-5 (EHCI -> OHCI fallback)
+		 */
+		if (ret == -ENODEV || rtwdev->fail) {
+			rtw_warn(rtwdev, "USB device gone after reset (ret=%d), aborting restart_hw\n", ret);
+			rtwdev->fail = false;
+			atomic_set(&rtwdev->fail_count, 0);
+			return;
+		}
+
+		rtwdev->fail = false;
+		atomic_set(&rtwdev->fail_count, 0);
+	}
 
 	mutex_lock(&rtwdev->mutex);
 	__fw_recovery_work(rtwdev);
 	mutex_unlock(&rtwdev->mutex);
 
-	ieee80211_restart_hw(rtwdev->hw);
+	/* Only restart if device still present */
+	if (!rtwdev->fail && rtwusb && rtwusb->udev) {
+		rtwdev->fail = false;
+		atomic_set(&rtwdev->fail_count, 0);
+		ieee80211_restart_hw(rtwdev->hw);
+	}
 }
 
 struct rtw_txq_ba_iter_data {
@@ -2169,6 +2220,10 @@ int rtw_core_init(struct rtw_dev *rtwdev
 	mutex_init(&rtwdev->mutex);
 	mutex_init(&rtwdev->hal.tx_power_mutex);
 
+	/* for -110 storm detection: stop hammering 0x4e0 / 0xaa */
+	rtwdev->fail = false;
+	atomic_set(&rtwdev->fail_count, 0);
+
 	init_waitqueue_head(&rtwdev->coex.wait);
 	init_completion(&rtwdev->lps_leave_check);
 	init_completion(&rtwdev->fw_scan_density);
--- a/drivers/net/wireless/realtek/rtw88/usb.c.orig	2026-08-22 16:54:34.585944753 -0700
+++ b/drivers/net/wireless/realtek/rtw88/usb.c	2026-08-22 17:02:26.565275839 -0700
@@ -27,6 +27,17 @@ struct rtw_usb_txcb {
 	struct sk_buff_head tx_ack_queue;
 };
 
+static void rtw_usb_mark_fail(struct rtw_dev *rtwdev, int status)
+{
+	if (status == -110 || status == -ENODEV || status == -EPROTO || status == -2) {
+		if (atomic_inc_return(&rtwdev->fail_count) >= 3 && !rtwdev->fail) {
+			rtwdev->fail = true;
+			rtw_warn(rtwdev, "-110 storm detected, scheduling reset from rtw_usb_mark_fail\n");
+			schedule_work(&rtwdev->fw_recovery_work);
+		}
+	}
+}
+
 static void rtw_usb_fill_tx_checksum(struct rtw_usb *rtwusb,
 				     struct sk_buff *skb, int agg_num)
 {
@@ -60,13 +71,20 @@ static void rtw_usb_reg_sec(struct rtw_d
 	if (!reg_on_section)
 		return;
 
+	if (unlikely(rtwdev->fail))
+		return;
+
 	status = usb_control_msg(udev, usb_sndctrlpipe(udev, 0),
 				 RTW_USB_CMD_REQ, RTW_USB_CMD_WRITE,
 				 t_reg, 0, data, t_len, 500);
 
-	if (status != t_len && status != -ENODEV)
+	if (status != t_len && status != -ENODEV) {
 		rtw_err(rtwdev, "%s: reg 0x%x, usb write %u fail, status: %d\n",
 			__func__, t_reg, t_len, status);
+		rtw_usb_mark_fail(rtwdev, status);
+	} else {
+		atomic_set(&rtwdev->fail_count, 0);
+	}
 }
 
 static u32 rtw_usb_read(struct rtw_dev *rtwdev, u32 addr, u16 len)
@@ -78,11 +96,12 @@ static u32 rtw_usb_read(struct rtw_dev *
 	int idx, ret;
 	static int count;
 
-	spin_lock_irqsave(&rtwusb->usb_lock, flags);
+	if (unlikely(rtwdev->fail))
+		return 0;
 
+	spin_lock_irqsave(&rtwusb->usb_lock, flags);
 	idx = rtwusb->usb_data_index;
 	rtwusb->usb_data_index = (idx + 1) & (RTW_USB_MAX_RXTX_COUNT - 1);
-
 	spin_unlock_irqrestore(&rtwusb->usb_lock, flags);
 
 	data = &rtwusb->usb_data[idx];
@@ -94,6 +113,11 @@ static u32 rtw_usb_read(struct rtw_dev *
 		rtw_err(rtwdev, "read register 0x%x failed with %d\n",
 			addr, ret);
 
+	if (ret < 0)
+		rtw_usb_mark_fail(rtwdev, ret);
+	else
+		atomic_set(&rtwdev->fail_count, 0);
+
 	if (rtwdev->chip->id == RTW_CHIP_TYPE_8822C ||
 	    rtwdev->chip->id == RTW_CHIP_TYPE_8822B ||
 	    rtwdev->chip->id == RTW_CHIP_TYPE_8821C)
@@ -114,6 +138,8 @@ static u16 rtw_usb_read16(struct rtw_dev
 
 static u32 rtw_usb_read32(struct rtw_dev *rtwdev, u32 addr)
 {
+	if (unlikely(rtwdev->fail))
+		return 0;
 	return (u32)rtw_usb_read(rtwdev, addr, 4);
 }
 
@@ -126,15 +152,15 @@ static void rtw_usb_write(struct rtw_dev
 	int idx, ret;
 	static int count;
 
-	spin_lock_irqsave(&rtwusb->usb_lock, flags);
+	if (unlikely(rtwdev->fail))
+		return;
 
+	spin_lock_irqsave(&rtwusb->usb_lock, flags);
 	idx = rtwusb->usb_data_index;
 	rtwusb->usb_data_index = (idx + 1) & (RTW_USB_MAX_RXTX_COUNT - 1);
-
 	spin_unlock_irqrestore(&rtwusb->usb_lock, flags);
 
 	data = &rtwusb->usb_data[idx];
-
 	*data = cpu_to_le32(val);
 
 	ret = usb_control_msg(udev, usb_sndctrlpipe(udev, 0),
@@ -142,7 +168,12 @@ static void rtw_usb_write(struct rtw_dev
 			      addr, 0, data, len, 500);
 	if (ret < 0 && ret != -ENODEV && count++ < 4)
 		rtw_err(rtwdev, "write register 0x%x failed with %d\n",
-			addr, ret);
+		        addr, ret);
+
+	if (ret < 0)
+		rtw_usb_mark_fail(rtwdev, ret);
+	else
+		atomic_set(&rtwdev->fail_count, 0);
 
 	if (rtwdev->chip->id == RTW_CHIP_TYPE_8822C ||
 	    rtwdev->chip->id == RTW_CHIP_TYPE_8822B ||
@@ -162,6 +193,8 @@ static void rtw_usb_write16(struct rtw_d
 
 static void rtw_usb_write32(struct rtw_dev *rtwdev, u32 addr, u32 val)
 {
+	if (unlikely(rtwdev->fail))
+		return;
 	rtw_usb_write(rtwdev, addr, val, 4);
 }
 
@@ -400,10 +433,16 @@ static bool rtw_usb_tx_agg_skb(struct rt
 	unsigned int align_next = 0;
 	u8 qsel;
 	int ret;
+	bool mark_fail = false;
 
 	if (skb_queue_empty(list))
 		return false;
 
+	if (unlikely(rtwdev->fail)) {
+		__skb_queue_purge(list);
+		return false;
+	}
+
 	txcb = kmalloc(sizeof(*txcb), GFP_ATOMIC);
 	if (!txcb)
 		return false;
@@ -460,9 +499,22 @@ queue:
 	ret = rtw_usb_write_port(rtwdev, qsel, skb_head,
 				 rtw_usb_write_port_tx_complete, txcb);
 	if (ret) {
-		ieee80211_purge_tx_queue(rtwdev->hw, &txcb->tx_ack_queue);
-		kfree(txcb);
-		return false;
+		/* FIX deadlock: splice then purge outside lock */
+		struct sk_buff_head free_q;
+		__skb_queue_head_init(&free_q);
+		skb_queue_splice_init(&txcb->tx_ack_queue, &free_q);
+
+		if (ret == -110 || ret == -ENODEV || ret == -EPROTO || ret == -ESHUTDOWN)
+			mark_fail = true;
+
+		ieee80211_purge_tx_queue(rtwdev->hw, &free_q);
+		skb_queue_purge(&free_q);
+ 		kfree(txcb);
+
+		if (mark_fail)
+			rtw_usb_mark_fail(rtwdev, ret);
+
+ 		return false;
 	}
 
 	return true;
@@ -523,11 +575,28 @@ static int rtw_usb_write_data(struct rtw
 	rtw_tx_fill_tx_desc(rtwdev, pkt_info, skb);
 	rtw_tx_fill_txdesc_checksum(rtwdev, pkt_info, skb->data);
 
+	/* If device already marked failed from previous -110 storm,
+	 * don't try to submit more URBs - free immediately.
+	 * This stops the log spam you saw:
+	 * read 0xaa failed -110, write 0x4e0 fail -110, etc.
+	 */
+	if (unlikely(rtwdev->fail)) {
+		dev_kfree_skb_any(skb);
+		return -ENODEV;
+	}
+
 	ret = rtw_usb_write_port(rtwdev, qsel, skb,
 				 rtw_usb_write_port_complete, skb);
 	if (unlikely(ret)) {
 		rtw_err(rtwdev, "failed to do USB write, ret=%d\n", ret);
+		/* 6b964941 fixed leak by adding kfree here, but caller
+		 * may hold rtwdev->mutex. Completion callback will NOT run
+		 * on failure, so we must free. Freeing here is OK because
+		 * this function does NOT hold mutex itself - caller does
+		 * unlock after return. Keep free outside any spinlock.
+		 */
 		dev_kfree_skb_any(skb);
+		rtw_usb_mark_fail(rtwdev, ret);
 	}
 
 	return ret;
--- a/drivers/net/wireless/realtek/rtw88/usb.h.orig	2025-11-30 14:42:10.000000000 -0800
+++ b/drivers/net/wireless/realtek/rtw88/usb.h	2026-08-22 17:02:26.565612918 -0700
@@ -99,5 +99,7 @@ static inline struct rtw_usb_tx_data *rt
 
 int rtw_usb_probe(struct usb_interface *intf, const struct usb_device_id *id);
 void rtw_usb_disconnect(struct usb_interface *intf);
+int __rtw_usb_read32(struct rtw_dev *rtwdev, u32 addr, u32 *val);
+int __rtw_usb_write32(struct rtw_dev *rtwdev, u32 addr, u32 val);
 
 #endif
--- a/net/mac80211/mlme.c.orig	2026-08-22 16:54:45.855375398 -0700
+++ b/net/mac80211/mlme.c	2026-08-22 17:02:26.566072688 -0700
@@ -3222,14 +3222,32 @@ static u64 ieee80211_handle_pwr_constr(s
 		has_80211h_pwr = ieee80211_find_80211h_pwr_constr(
 			channel, country_ie, country_ie_len,
 			pwr_constr_ie, &chan_pwr, &pwr_reduction_80211h);
-		pwr_level_80211h =
-			max_t(int, 0, chan_pwr - pwr_reduction_80211h);
+
+		/* FIX: 127 is invalid, AP <redacted> advertises 127 as "no limit"
+		 * This wedges rtw88 -> read 0x770 fails -110
+		 */
+		if (chan_pwr == 127 || chan_pwr > 30) {
+			sdata_dbg(sdata,
+				  "Ignore invalid TX power %d as advertised by %pM\n",
+				  chan_pwr, link->u.mgd.bssid);
+			has_80211h_pwr = false;
+		} else {
+			pwr_level_80211h =
+				max_t(int, 0, chan_pwr - pwr_reduction_80211h);
+			if (pwr_level_80211h == 127 || pwr_level_80211h > 30) {
+				has_80211h_pwr = false;
+			}
+		}
 	}
 
 	if (cisco_dtpc_ie) {
 		ieee80211_find_cisco_dtpc(
 			channel, cisco_dtpc_ie, &pwr_level_cisco);
-		has_cisco_pwr = true;
+		/* same for Cisco */
+		if (pwr_level_cisco == 127 || pwr_level_cisco > 30)
+			has_cisco_pwr = false;
+		else
+			has_cisco_pwr = true;
 	}
 
 	if (!has_80211h_pwr && !has_cisco_pwr)
