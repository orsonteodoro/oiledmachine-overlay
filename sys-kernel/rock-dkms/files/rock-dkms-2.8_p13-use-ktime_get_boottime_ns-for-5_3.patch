--- a/amd/amdkfd/kfd_chardev.c.orig	2019-10-03 05:15:41.500120018 -0700
+++ b/amd/amdkfd/kfd_chardev.c	2019-10-03 06:42:48.664572046 -0700
@@ -857,7 +857,9 @@ static int kfd_ioctl_get_clock_counters(
 	args->cpu_clock_counter = (uint64_t)timespec_to_ns(&time);
 #endif
 
-#if LINUX_VERSION_CODE >= KERNEL_VERSION(4, 17, 0)
+#if LINUX_VERSION_CODE >= KERNEL_VERSION(5, 3, 0)
+	args->system_clock_counter = ktime_get_boottime_ns();
+#elif LINUX_VERSION_CODE >= KERNEL_VERSION(4, 17, 0) && LINUX_VERSION_CODE < KERNEL_VERSION(5, 3, 0)
 	args->system_clock_counter = ktime_get_boot_ns();
 #elif LINUX_VERSION_CODE >= KERNEL_VERSION(3, 17, 0)
 	get_monotonic_boottime64(&time);
