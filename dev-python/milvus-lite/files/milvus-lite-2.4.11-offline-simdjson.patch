diff '--color=auto' -urp milvus-lite-2.4.11.orig/CMakeLists.txt milvus-lite-2.4.11/CMakeLists.txt
--- milvus-lite-2.4.11.orig/CMakeLists.txt	2025-02-04 09:43:14.093792204 -0800
+++ milvus-lite-2.4.11/CMakeLists.txt	2025-02-04 09:46:33.240290252 -0800
@@ -244,11 +244,21 @@ add_library(
   boost_bitset_ext
   thirdparty/milvus/internal/core/thirdparty/boost_ext/dynamic_bitset_ext.cpp)
 
-FetchContent_Declare(
-  simdjson
-  GIT_REPOSITORY https://github.com/simdjson/simdjson.git
-  GIT_TAG v3.1.7)
-FetchContent_MakeAvailable(simdjson)
+option(OFFLINE "Run offline mode?" ON)
+if(OFFLINE)
+  FetchContent_Declare(
+    simdjson
+    SOURCE_DIR "${CMAKE_SOURCE_DIR}/thirdparty/simdjson"
+    BINARY_DIR "${CMAKE_CURRENT_BINARY_DIR}/knowhere-build"
+  )
+  FetchContent_MakeAvailable(simdjson)
+else()
+  FetchContent_Declare(
+    simdjson
+    GIT_REPOSITORY https://github.com/simdjson/simdjson.git
+    GIT_TAG v3.1.7)
+  FetchContent_MakeAvailable(simdjson)
+endif()
 
 if(CMAKE_BUILD_TYPE STREQUAL "Debug")
   set(CARGO_CMD cargo build)
