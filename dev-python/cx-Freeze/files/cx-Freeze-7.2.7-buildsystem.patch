--- a/setup.py.orig	2024-11-29 13:26:53.000000000 -0800
+++ b/setup.py	2024-12-17 09:53:54.055356131 -0800
@@ -117,8 +117,6 @@ class BuildBases(setuptools.command.buil
                     extra_args.extend(get_config_var("LINKFORSHARED").split())
                 extra_args.append("-Wl,-rpath,$ORIGIN/lib")
                 extra_args.append("-Wl,-rpath,$ORIGIN/../lib")
-                if not self.debug:
-                    extra_args.append("-s")
         link_error = None
         for arg in (None, "-fno-lto", "--no-lto"):
             try:
