#!/bin/bash
which wmname && pidof dwm && ( wmname LG3D & )
ENIGMA_PREFIX="/usr/lib/enigma"
JNA_JAR="JNA_PATH"
cd "${ENIGMA_PREFIX}"
export LD_LIBRARY_PATH="${ENIGMA_PREFIX}:${LD_LIBRARY_PATH}"
java \
	-noverify \
	-cp .:${JNA_JAR} \
	-jar "${ENIGMA_PREFIX}/lateralgm.jar" \
	"${@}"
