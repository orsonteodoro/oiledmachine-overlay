<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
  <maintainer type="person">
    <!-- Only for this ebuild on the oiledmachine-overlay -->
    <email>orsonteodoro@hotmail.com</email>
    <name>Orson Teodoro</name>
  </maintainer>
  <longdescription>

    Required environment variables:

      MACOS_SDK_PV - The macOS SDK version
      OSXCROSS_ROOT - Path to OSXCross with target as a subfolder
      OSXCROSS_CHOST - The CHOST prefix to clang, cpp, etc

  </longdescription>
  <use>
    <flag name="box2">Adds Box2D support for 2D physics support.</flag>
    <flag name="bullet">Adds Bullet support for 3D physics support.</flag>
    <flag name="curl">Adds cURL support for multiplayer support.</flag>
    <flag name="doc">Installs documents.</flag>
    <flag name="externalfuncs">Add support for GM ExternalFuncs to call DLL
      functions.</flag>
    <flag name="gles1">Installs and adds OpenGL ES 1.x support.</flag>
    <flag name="gles3">Installs and adds OpenGL ES 3.x support.</flag>
    <flag name="gme">Checks for proper dependencies for support for
      music formats used in 8-bit/16-bit console systems.</flag>
    <flag name="gnome">Uses Zenity as the default widget engine if not using
      KDE for alerts, user prompts, file dialogs, color picker.</flag>
    <flag name="gtk2">Makes GTK+2 the default widget system for file dialogs,
      input dialogs, and color picker.</flag>
    <flag name="gtest">Adds support for gtest.</flag>
    <flag name="headless">Run as headless for building servers.</flag>
    <flag name="joystick">Add support for gamepads and joysticks.</flag>
    <flag name="jpeg">For linking only.  It likely does nothing.</flag>
    <flag name="kde">Uses KDialog as the default widget engine if running
      KDE for alerts, user prompts, file dialogs, color picker.</flag>
    <flag name="macos">Apply changes for support for cross compiling for
      macOS.</flag>
    <flag name="network">Adds dependencies for network support.</flag>
    <flag name="openal">Installs and makes OpenAL the default audio
      system.</flag>
    <flag name="opengl">Adds OpenGL support.</flag>
    <flag name="png">Adds support for sprites that use PNG images.</flag>
    <flag name="sdl2">Adds window creation and input support via SDL2 in a
      platform independent way.</flag>
    <flag name="test">Tests the code.</flag>
    <flag name="threads">Adds thread support.</flag>
    <flag name="widgets">Adds widgets support for dialogs.</flag>
    <flag name="X">Add window and input support through X11 in a platform
      dependent way.</flag>
    <flag name="xtest">Adds support for keyboard_key_release function
      for game scripting in X.</flag>
    <flag name="xrandr">Adds support for resizing/getting resolution
      and frequency.</flag>
  </use>
</pkgmetadata>
