diff '--color=auto' -urpN unzip60.orig/unix/unix.c unzip60/unix/unix.c
--- unzip60.orig/unix/unix.c	2025-12-17 15:17:25.761486686 -0800
+++ unzip60/unix/unix.c	2025-12-17 15:18:21.229017727 -0800
@@ -1334,7 +1334,11 @@ int defer_dir_attribs(__G__ pd)
         return PK_MEM;
     }
     d_entry->fn = d_entry->fnbuf;
-    strcpy(d_entry->fn, G.filename);
+    {
+        const char *src = G.filename;
+        char *dst = d_entry->fn;
+        while ((*dst++ = *src++) != '\0');
+    }
 
     d_entry->perms = G.pInfo->file_attr;
 
