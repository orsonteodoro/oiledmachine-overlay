Title: Rebuild the kernel to mitigate against 0-day vulnerabilities
Author: Orson Teodoro <orsonteodoro@hotmail.com>
Content-Type: text/plain
Posted: 2026-05-09
Revision: 5
News-Item-Format: 2.0
Display-If-Installed: sys-kernel/mitigate-dos
Display-If-Installed: sys-kernel/mitigate-dt
Display-If-Installed: sys-kernel/mitigate-id
Display-If-Installed: sys-kernel/ot-sources
Display-If-Installed: virtual/dss
Display-If-Installed: virtual/ot-sources-stable
Display-If-Installed: virtual/ot-sources-lts

The Linux kernel had several zero day vulnerabilities:

CVE-2026-43284, CVE-2026-43500 - Dirty Frag:  privilege escalation, attack chain primitive
CVE-2026-31431 - Copy Fail:  priviliege escalation, container breakout, attack chain primitive

CVE-2026-43284 - CVSS:3.1/AV:L/AC:H/PR:L/UI:N/S:C/C:H/I:H/A:H (denial of service, data tampering, information disclosure)
CVE-2026-31431 - CVSS:3.1/AV:L/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:H (denial of service, data tampering, information disclosure)

The AV:L (locally ran) is deceptive, it can be chained together with another
exploited vulnerability with remote code execution for full system compromise.
The chances increase if you haven't updated your kernel for months or years or
through red zone apps.

Both are mitigated.  The official patch fix for Dirty Frag is currently
partially done, so disabling the affected module is the mitigation fallback.

Always update the initramfs with updated kernel modules to avoid getting a
black bootup screen.

Remember to keep backup kernels.  Do not delete old kernels until you verify
that the kernel works.

Verify the following kernel versions for partial/full mitigation

>= 7.0.5
>= 6.18.28
>= 6.12.87
>= 6.6.138
>= 6.1.172
>= 5.15.206
>= 5.10.255

Ways to mitigate the remaining:

Disable or comment out in .config for the latest point releases

CONFIG_AF_RXRPC

or 

Unload rxrpc kernel module if built as a kernel module.

or

For older kernels you need all three disabled in the .config or unload all the modules:

CONFIG_AF_RXRPC
CONFIG_INET_ESP
CONFIG_INET6_ESP

or

Unload or blacklist all the modules if external modules:

modprobe -r esp4 
modprobe -r esp6
modprobe -r rxrpc

The config disable is preferred to eliminate the vulnerable time window before use.

For the Copy Fail, you need at least the following for full mitigation:

7.0.3 (stable):  Patched
6.18.26 (LTS):  Patched
6.12.85 (LTS):  Patched
6.6.137 (LTS):  Patched
6.1.170 (LTS):  Patched
5.15.204 (LTS):  Patched
5.10.254 (LTS):  Patched

Still vulnerable hunks were observed:

<= 6.18.21 (LTS):  Not patched
<= 6.12.84 (LTS):  Not patched
<= 6.6.136 (LTS):  Not patched
<= 6.1.169 (LTS):  Not patched
<= 5.15.203 (LTS):  Not patched
<= 5.10.253 (LTS):  Not patched

Both Copy Fail and Dirty frag should be mitigated ASAP before the KEV deadline
on the 15 of this month, but it is recommend earlier if meeting billing
commitments or transfering very sensitive data before this time.
