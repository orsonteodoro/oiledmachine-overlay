# This patch contains AI generated code.
diff '--color=auto' -urpN a/src-tauri/capabilities/default.json b/src-tauri/capabilities/default.json
--- a/src-tauri/capabilities/default.json	1969-12-31 16:00:00.000000000 -0800
+++ b/src-tauri/capabilities/default.json	2026-07-31 15:19:33.780371851 -0700
@@ -0,0 +1,11 @@
+{
+  "$schema": "../gen/schemas/desktop-schema.json",
+  "identifier": "main-capability",
+  "description": "Capability for the main window",
+  "windows": ["main"],
+  "permissions": [
+    "core:default",
+    "core:tray:default",
+    "shell:allow-open"
+  ]
+}
diff '--color=auto' -urpN a/src-tauri/src/main.rs b/src-tauri/src/main.rs
--- a/src-tauri/src/main.rs	2025-07-23 13:57:17.000000000 -0700
+++ b/src-tauri/src/main.rs	2026-07-31 15:19:33.780513943 -0700
@@ -1,51 +1,64 @@
 // Prevents additional console window on Windows in release, DO NOT REMOVE!!
 #![cfg_attr(not(debug_assertions), windows_subsystem = "windows")]
 
-use tauri::{
-  CustomMenuItem,
-  SystemTray,
-  SystemTrayEvent,
-  SystemTrayMenu,
-  SystemTrayMenuItem,
-};
 use tauri::Manager;
+use tauri::menu::{Menu, MenuItem};
+use tauri::tray::{TrayIconBuilder, TrayIconEvent};
+// Import the shell extension trait to use app_handle.shell()
+use tauri_plugin_shell::ShellExt;
+use tauri::menu::PredefinedMenuItem;
 
 #[tauri::command]
 async fn close_splashscreen(window: tauri::Window) {
   // Close splashscreen
-  window.get_window("splashscreen").expect("no window labeled 'splashscreen' found").close().unwrap();
+  window.get_webview_window("splashscreen").expect("no window labeled 'splashscreen' found").close().unwrap();
   // Show main window
-  window.get_window("main").expect("no window labeled 'main' found").show().unwrap();
+  window.get_webview_window("main").expect("no window labeled 'main' found").show().unwrap();
 }
 
 
 fn main() {
   tauri::Builder::default()
-    .system_tray(SystemTray::new()
-      .with_menu(SystemTrayMenu::new()
-        .add_item(CustomMenuItem::new("checkforupdates".to_string(), "Check for updates"))
-        .add_native_item(SystemTrayMenuItem::Separator)
-        .add_item(CustomMenuItem::new("help".to_string(), "Help"))
-        .add_native_item(SystemTrayMenuItem::Separator)
-        .add_item(CustomMenuItem::new("quit".to_string(), "Quit"))
-      )
-    )
-    .on_system_tray_event(|app, event| match event {
-      SystemTrayEvent::MenuItemClick { id, .. } => {
-        match id.as_str() {
-          "quit" => {
-            std::process::exit(0);
-          }
-          "checkforupdates" => {
-            tauri::api::shell::open(&app.shell_scope(), "https://github.com/semperai/amica/releases/latest", None).expect("failed to open url");
-          }
-          "help" => {
-            tauri::api::shell::open(&app.shell_scope(), "https://docs.heyamica.com", None).expect("failed to open url");
+    // 1. Initialize the shell plugin
+    .plugin(tauri_plugin_shell::init())
+    .setup(|app| {
+      // 2. Rebuild the menu items using the new MenuItem API
+      let check_updates = MenuItem::with_id(app, "checkforupdates", "Check for updates", true, None::<&str>)?;
+      let help = MenuItem::with_id(app, "help", "Help", true, None::<&str>)?;
+      let quit = MenuItem::with_id(app, "quit", "Quit", true, None::<&str>)?;
+
+      // 3. Construct the menu and add items/separators sequentially
+      let tray_menu = Menu::with_items(app, &[
+        &check_updates,
+        &PredefinedMenuItem::separator(app)?,
+        &help,
+        &PredefinedMenuItem::separator(app)?,
+        &quit,
+      ])?;
+
+      // 4. Build the Tray Icon and attach the menu
+      let _tray = TrayIconBuilder::new()
+        .icon(app.default_window_icon().unwrap().clone())
+        .menu(&tray_menu)
+        // 5. Handle click events using the new TrayIconEvent API
+        .on_menu_event(|app_handle, event| {
+          match event.id.as_ref() {
+            "quit" => {
+              std::process::exit(0);
+            }
+            "checkforupdates" => {
+              // Open URLs via the new plugin extension
+              let _ = app_handle.shell().open("https://github.com/semperai/amica/releases/latest", None);
+            }
+            "help" => {
+              let _ = app_handle.shell().open("https://docs.heyamica.com", None);
+            }
+            _ => {}
           }
-          _ => {}
-        }
-      }
-      _ => {}
+        })
+        .build(app)?;
+
+      Ok(())
     })
     .invoke_handler(tauri::generate_handler![close_splashscreen])
     .run(tauri::generate_context!())
diff '--color=auto' -urpN a/src-tauri/src-tauri/capabilities/default.json b/src-tauri/src-tauri/capabilities/default.json
--- a/src-tauri/src-tauri/capabilities/default.json	1969-12-31 16:00:00.000000000 -0800
+++ b/src-tauri/src-tauri/capabilities/default.json	2026-07-31 15:19:26.934327981 -0700
@@ -0,0 +1,11 @@
+{
+  "$schema": "../gen/schemas/desktop-schema.json",
+  "identifier": "main-capability",
+  "description": "Capability for the main window",
+  "windows": ["main"],
+  "permissions": [
+    "core:default",
+    "tray:default",
+    "shell:allow-open"
+  ]
+}
diff '--color=auto' -urpN a/src-tauri/tauri.conf.json b/src-tauri/tauri.conf.json
--- a/src-tauri/tauri.conf.json	2025-07-23 13:57:17.000000000 -0700
+++ b/src-tauri/tauri.conf.json	2026-07-31 15:21:00.859343817 -0700
@@ -1,66 +1,16 @@
 {
   "$schema": "../node_modules/@tauri-apps/cli/schema.json",
+  "productName": "amica",
+  "version": "0.2.1",
+  "identifier": "com.heyamica.dev",
   "build": {
     "beforeBuildCommand": "export NEXT_OUTPUT=\"export\" && npm run build && npm run splash",
     "beforeDevCommand": "npm run dev",
-    "devPath": "http://localhost:3000",
-    "distDir": "../out"
+    "devUrl": "http://localhost:3000",
+    "frontendDist": "../out"
   },
-  "package": {
-    "productName": "amica",
-    "version": "0.2.1"
-  },
-  "tauri": {
-    "allowlist": {
-      "all": false,
-      "shell": {
-        "open": true
-      }
-    },
-    "bundle": {
-      "active": true,
-      "category": "DeveloperTool",
-      "copyright": "",
-      "deb": {
-        "depends": []
-      },
-      "externalBin": [],
-      "icon": [
-        "icons/32x32.png",
-        "icons/128x128.png",
-        "icons/128x128@2x.png",
-        "icons/icon.icns",
-        "icons/icon.ico"
-      ],
-      "identifier": "com.heyamica.dev",
-      "longDescription": "",
-      "macOS": {
-        "entitlements": "Release.entitlements",
-        "exceptionDomain": "",
-        "frameworks": [],
-        "providerShortName": null,
-        "signingIdentity": null
-      },
-      "resources": [],
-      "shortDescription": "",
-      "targets": "all",
-      "windows": {
-        "certificateThumbprint": null,
-        "digestAlgorithm": "sha256",
-        "timestampUrl": ""
-      }
-    },
+  "app": {
     "macOSPrivateApi": true,
-    "security": {
-      "csp": null
-    },
-    "systemTray": {
-      "iconPath": "icons/icon.png",
-      "iconAsTemplate": true
-    },
-    "updater": {
-      "active": false
-    },
     "windows": [
       {
         "width": 800,
@@ -82,6 +32,38 @@
         "url": "/splashscreen.html",
         "label": "splashscreen"
       }
-    ]
+    ],
+    "security": {
+      "csp": null
+    }
+  },
+  "bundle": {
+    "active": true,
+    "category": "DeveloperTool",
+    "copyright": "",
+    "externalBin": [],
+    "icon": [
+      "icons/32x32.png",
+      "icons/128x128.png",
+      "icons/128x128@2x.png",
+      "icons/icon.icns",
+      "icons/icon.ico"
+    ],
+    "longDescription": "",
+    "macOS": {
+      "entitlements": "Release.entitlements",
+      "exceptionDomain": "",
+      "frameworks": [],
+      "providerShortName": null,
+      "signingIdentity": null
+    },
+    "resources": [],
+    "shortDescription": "",
+    "targets": "deb",
+    "windows": {
+      "certificateThumbprint": null,
+      "digestAlgorithm": "sha256",
+      "timestampUrl": ""
+    }
   }
 }
--- a/src/components/vrmViewer.tsx.orig	2025-07-23 13:57:17.000000000 -0700
+++ b/src/components/vrmViewer.tsx	2026-07-31 16:32:42.479606316 -0700
@@ -5,7 +5,7 @@ import { buildUrl } from "@/utils/buildU
 import { config } from "@/utils/config";
 import { useVrmStoreContext } from "@/features/vrmStore/vrmStoreContext";
 import isTauri from "@/utils/isTauri";
-import { invoke } from "@tauri-apps/api/tauri";
+import { invoke } from "@tauri-apps/api/core";
 import { ChatContext } from "@/features/chat/chatContext";
 import clsx from "clsx";
 
