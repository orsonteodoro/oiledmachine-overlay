# This patch contains AI generated code.
diff '--color=auto' -urp a/src/components/vrmViewer.tsx b/src/components/vrmViewer.tsx
--- a/src/components/vrmViewer.tsx	2025-07-23 13:57:17.000000000 -0700
+++ b/src/components/vrmViewer.tsx	2026-07-31 19:16:22.806049352 -0700
@@ -5,7 +5,7 @@ import { buildUrl } from "@/utils/buildU
 import { config } from "@/utils/config";
 import { useVrmStoreContext } from "@/features/vrmStore/vrmStoreContext";
 import isTauri from "@/utils/isTauri";
-import { invoke } from "@tauri-apps/api/tauri";
+import { invoke } from "@tauri-apps/api/core";
 import { ChatContext } from "@/features/chat/chatContext";
 import clsx from "clsx";
 
Only in b/src-tauri: capabilities
diff '--color=auto' -urp a/src-tauri/src/main.rs b/src-tauri/src/main.rs
--- a/src-tauri/src/main.rs	2025-07-23 13:57:17.000000000 -0700
+++ b/src-tauri/src/main.rs	2026-07-31 19:26:51.163877613 -0700
@@ -1,53 +1,68 @@
 // Prevents additional console window on Windows in release, DO NOT REMOVE!!
 #![cfg_attr(not(debug_assertions), windows_subsystem = "windows")]
 
-use tauri::{
-  CustomMenuItem,
-  SystemTray,
-  SystemTrayEvent,
-  SystemTrayMenu,
-  SystemTrayMenuItem,
-};
 use tauri::Manager;
+use tauri::menu::{Menu, MenuItem};
+use tauri::tray::{TrayIconBuilder, TrayIconEvent, PredefinedMenuItem};
+// Import the shell extension trait to use app_handle.shell()
+use tauri_plugin_shell::ShellExt;
 
-#[tauri::command]
-async fn close_splashscreen(window: tauri::Window) {
-  // Close splashscreen
-  window.get_window("splashscreen").expect("no window labeled 'splashscreen' found").close().unwrap();
-  // Show main window
-  window.get_window("main").expect("no window labeled 'main' found").show().unwrap();
-}
+fn main() {
+    tauri::Builder::default()
+        .plugin(tauri_plugin_shell::init())
+        .setup(|app| {
+            // 1. Get a thread-safe handle to the application
+            let handle = app.handle().clone();
 
+            // 2. Build the Menu and Tray immediately on the main thread (fixes scoping & dropping)
+            let check_updates = MenuItem::with_id(app, "checkforupdates", "Check for updates", true, None::<&str>)?;
+            let help = MenuItem::with_id(app, "help", "Help", true, None::<&str>)?;
+            let quit = MenuItem::with_id(app, "quit", "Quit", true, None::<&str>)?;
 
-fn main() {
-  tauri::Builder::default()
-    .system_tray(SystemTray::new()
-      .with_menu(SystemTrayMenu::new()
-        .add_item(CustomMenuItem::new("checkforupdates".to_string(), "Check for updates"))
-        .add_native_item(SystemTrayMenuItem::Separator)
-        .add_item(CustomMenuItem::new("help".to_string(), "Help"))
-        .add_native_item(SystemTrayMenuItem::Separator)
-        .add_item(CustomMenuItem::new("quit".to_string(), "Quit"))
-      )
-    )
-    .on_system_tray_event(|app, event| match event {
-      SystemTrayEvent::MenuItemClick { id, .. } => {
-        match id.as_str() {
-          "quit" => {
-            std::process::exit(0);
-          }
-          "checkforupdates" => {
-            tauri::api::shell::open(&app.shell_scope(), "https://github.com/semperai/amica/releases/latest", None).expect("failed to open url");
-          }
-          "help" => {
-            tauri::api::shell::open(&app.shell_scope(), "https://docs.heyamica.com", None).expect("failed to open url");
-          }
-          _ => {}
-        }
-      }
-      _ => {}
-    })
-    .invoke_handler(tauri::generate_handler![close_splashscreen])
-    .run(tauri::generate_context!())
-    .expect("error while running tauri application");
+            let tray_menu = Menu::with_items(app, &[
+                &check_updates,
+                &PredefinedMenuItem::separator(app)?,
+                &help,
+                &PredefinedMenuItem::separator(app)?,
+                &quit,
+            ])?;
+
+            // We assign it to an app-scoped state or let it leak so it stays alive
+            let _tray = TrayIconBuilder::new()
+                .icon(app.default_window_icon().unwrap().clone())
+                .menu(&tray_menu)
+                .on_menu_event(|app_handle: &tauri::AppHandle, event| {
+                    match event.id.as_ref() {
+                        "quit" => {
+                            app_handle.exit(0); // Cleaner than std::process::exit
+                        }
+                        "checkforupdates" => {
+                            let _ = app_handle.shell().open("https://github.com/semperai/amica/releases/latest", None);
+                        }
+                        "help" => {
+                            let _ = app_handle.shell().open("https://docs.heyamica.com", None);
+                        }
+                        _ => {}
+                    }
+                })
+                .build(app)?;
+
+            // 3. Grab window handles using the v2 method
+            let splashscreen = app.get_webview_window("splashscreen").unwrap();
+            let main_window = app.get_webview_window("main").unwrap();
+
+            // 4. Run your loading delay asynchronously
+            tauri::async_runtime::spawn(async move {
+                // Simulate loading/heavy tasks
+                tokio::time::sleep(std::time::Duration::from_secs(3)).await;
+
+                // Close splash and open main window using the moved window variables
+                let _ = splashscreen.close();
+                let _ = main_window.show();
+            });
+
+            Ok(())
+        })
+        .run(tauri::generate_context!())
+        .expect("error while running tauri application");
 }
Only in b/src-tauri: b/src-tauri
diff '--color=auto' -urp a/src-tauri/tauri.conf.json b/src-tauri/tauri.conf.json
--- a/src-tauri/tauri.conf.json	2025-07-23 13:57:17.000000000 -0700
+++ b/src-tauri/tauri.conf.json	2026-07-31 19:16:22.805897089 -0700
@@ -1,66 +1,16 @@
 {
   "$schema": "../node_modules/@tauri-apps/cli/schema.json",
+  "productName": "amica",
+  "version": "0.2.1",
+  "identifier": "com.heyamica.dev",
   "build": {
     "beforeBuildCommand": "export NEXT_OUTPUT=\"export\" && npm run build && npm run splash",
     "beforeDevCommand": "npm run dev",
-    "devPath": "http://localhost:3000",
-    "distDir": "../out"
+    "devUrl": "http://localhost:3000",
+    "frontendDist": "../out"
   },
-  "package": {
-    "productName": "amica",
-    "version": "0.2.1"
-  },
-  "tauri": {
-    "allowlist": {
-      "all": false,
-      "shell": {
-        "open": true
-      }
-    },
-    "bundle": {
-      "active": true,
-      "category": "DeveloperTool",
-      "copyright": "",
-      "deb": {
-        "depends": []
-      },
-      "externalBin": [],
-      "icon": [
-        "icons/32x32.png",
-        "icons/128x128.png",
-        "icons/128x128@2x.png",
-        "icons/icon.icns",
-        "icons/icon.ico"
-      ],
-      "identifier": "com.heyamica.dev",
-      "longDescription": "",
-      "macOS": {
-        "entitlements": "Release.entitlements",
-        "exceptionDomain": "",
-        "frameworks": [],
-        "providerShortName": null,
-        "signingIdentity": null
-      },
-      "resources": [],
-      "shortDescription": "",
-      "targets": "all",
-      "windows": {
-        "certificateThumbprint": null,
-        "digestAlgorithm": "sha256",
-        "timestampUrl": ""
-      }
-    },
+  "app": {
     "macOSPrivateApi": true,
-    "security": {
-      "csp": null
-    },
-    "systemTray": {
-      "iconPath": "icons/icon.png",
-      "iconAsTemplate": true
-    },
-    "updater": {
-      "active": false
-    },
     "windows": [
       {
         "width": 800,
@@ -82,6 +32,38 @@
         "url": "/splashscreen.html",
         "label": "splashscreen"
       }
-    ]
+    ],
+    "security": {
+      "csp": null
+    }
+  },
+  "bundle": {
+    "active": true,
+    "category": "DeveloperTool",
+    "copyright": "",
+    "externalBin": [],
+    "icon": [
+      "icons/32x32.png",
+      "icons/128x128.png",
+      "icons/128x128@2x.png",
+      "icons/icon.icns",
+      "icons/icon.ico"
+    ],
+    "longDescription": "",
+    "macOS": {
+      "entitlements": "Release.entitlements",
+      "exceptionDomain": "",
+      "frameworks": [],
+      "providerShortName": null,
+      "signingIdentity": null
+    },
+    "resources": [],
+    "shortDescription": "",
+    "targets": "deb",
+    "windows": {
+      "certificateThumbprint": null,
+      "digestAlgorithm": "sha256",
+      "timestampUrl": ""
+    }
   }
 }
