diff '--color=auto' -urp ollama-0.24.0.orig/api/client.go ollama-0.24.0/api/client.go
--- ollama-0.24.0.orig/api/client.go	2026-05-14 12:08:13.000000000 -0700
+++ ollama-0.24.0/api/client.go	2026-06-05 10:36:52.870181686 -0700
@@ -461,6 +461,7 @@ func (c *Client) Version(ctx context.Con
 
 // CloudStatusExperimental returns whether cloud features are disabled on the server.
 func (c *Client) CloudStatusExperimental(ctx context.Context) (*StatusResponse, error) {
+	return nil, errors.New(string("cloud support is disabled")) // DISABLE_CLOUD
 	var status StatusResponse
 	if err := c.do(ctx, http.MethodGet, "/api/status", nil, &status); err != nil {
 		return nil, err
diff '--color=auto' -urp ollama-0.24.0.orig/app/tools/cloud_policy.go ollama-0.24.0/app/tools/cloud_policy.go
--- ollama-0.24.0.orig/app/tools/cloud_policy.go	2026-05-14 12:08:13.000000000 -0700
+++ ollama-0.24.0/app/tools/cloud_policy.go	2026-06-05 10:37:31.021007672 -0700
@@ -17,6 +17,8 @@ func ensureCloudEnabledForTool(ctx conte
 	// the connected server's /api/status endpoint below.
 	disabledMessage := internalcloud.DisabledError(operation)
 
+	return errors.New(disabledMessage + " (cloud support is disabled)") // DISABLE_CLOUD
+
 	client, err := api.ClientFromEnvironment()
 	if err != nil {
 		return errors.New(disabledMessage + " (unable to verify server cloud policy)")
diff '--color=auto' -urp ollama-0.24.0.orig/cmd/interactive.go ollama-0.24.0/cmd/interactive.go
--- ollama-0.24.0.orig/cmd/interactive.go	2026-05-14 12:08:13.000000000 -0700
+++ ollama-0.24.0/cmd/interactive.go	2026-06-05 11:01:27.652788737 -0700
@@ -641,6 +641,7 @@ func extractFileData(input string) (stri
 }
 
 func editInExternalEditor(content string) (string, error) {
+	return "", fmt.Errorf("external editor support is disabled") // DISABLE_EXTERNAL_EDITOR
 	editor := envconfig.Editor()
 	if editor == "" {
 		editor = os.Getenv("VISUAL")
diff '--color=auto' -urp ollama-0.24.0.orig/cmd/launch/claude.go ollama-0.24.0/cmd/launch/claude.go
--- ollama-0.24.0.orig/cmd/launch/claude.go	2026-05-14 12:08:13.000000000 -0700
+++ ollama-0.24.0/cmd/launch/claude.go	2026-06-05 10:41:10.822243457 -0700
@@ -26,6 +26,7 @@ func (c *Claude) args(model string, extr
 }
 
 func (c *Claude) findPath() (string, error) {
+	return "", fmt.Errorf("claude support is disabled") // DISABLE_CLAUDE
 	if p, err := exec.LookPath("claude"); err == nil {
 		return p, nil
 	}
@@ -45,6 +46,7 @@ func (c *Claude) findPath() (string, err
 }
 
 func (c *Claude) Run(model string, args []string) error {
+	return fmt.Errorf("claude support is disabled") // DISABLE_CLAUDE
 	claudePath, err := c.findPath()
 	if err != nil {
 		return fmt.Errorf("claude is not installed, install from https://code.claude.com/docs/en/quickstart")
diff '--color=auto' -urp ollama-0.24.0.orig/cmd/launch/cline.go ollama-0.24.0/cmd/launch/cline.go
--- ollama-0.24.0.orig/cmd/launch/cline.go	2026-05-14 12:08:13.000000000 -0700
+++ ollama-0.24.0/cmd/launch/cline.go	2026-06-05 01:24:04.077694854 -0700
@@ -17,6 +17,7 @@ type Cline struct{}
 func (c *Cline) String() string { return "Cline" }
 
 func (c *Cline) Run(model string, args []string) error {
+	return fmt.Errorf("cline support is disabled"); // DISABLE_CLINE
 	if _, err := exec.LookPath("cline"); err != nil {
 		return fmt.Errorf("cline is not installed, install with: npm install -g cline")
 	}
diff '--color=auto' -urp ollama-0.24.0.orig/cmd/launch/codex.go ollama-0.24.0/cmd/launch/codex.go
--- ollama-0.24.0.orig/cmd/launch/codex.go	2026-05-14 12:08:13.000000000 -0700
+++ ollama-0.24.0/cmd/launch/codex.go	2026-06-05 01:23:52.026064873 -0700
@@ -38,6 +38,7 @@ func (c *Codex) args(model string, extra
 }
 
 func (c *Codex) Run(model string, args []string) error {
+	return fmt.Errorf("codex support is disabled") // DISABLE_CODEX
 	if err := checkCodexVersion(); err != nil {
 		return err
 	}
diff '--color=auto' -urp ollama-0.24.0.orig/cmd/launch/copilot.go ollama-0.24.0/cmd/launch/copilot.go
--- ollama-0.24.0.orig/cmd/launch/copilot.go	2026-05-14 12:08:13.000000000 -0700
+++ ollama-0.24.0/cmd/launch/copilot.go	2026-06-05 10:43:08.802612510 -0700
@@ -25,6 +25,7 @@ func (c *Copilot) args(model string, ext
 }
 
 func (c *Copilot) findPath() (string, error) {
+	return "", fmt.Errorf("copilot support is disabled") // DISABLE_COPILOT
 	if p, err := exec.LookPath("copilot"); err == nil {
 		return p, nil
 	}
@@ -44,6 +45,7 @@ func (c *Copilot) findPath() (string, er
 }
 
 func (c *Copilot) Run(model string, args []string) error {
+	return fmt.Errorf("copilot support is disabled") // DISABLE_COPILOT
 	copilotPath, err := c.findPath()
 	if err != nil {
 		return fmt.Errorf("copilot is not installed, install from https://docs.github.com/en/copilot/how-tos/set-up/install-copilot-cli")
diff '--color=auto' -urp ollama-0.24.0.orig/cmd/launch/droid.go ollama-0.24.0/cmd/launch/droid.go
--- ollama-0.24.0.orig/cmd/launch/droid.go	2026-05-14 12:08:13.000000000 -0700
+++ ollama-0.24.0/cmd/launch/droid.go	2026-06-05 01:48:14.445154480 -0700
@@ -41,6 +41,7 @@ type modelEntry struct {
 func (d *Droid) String() string { return "Droid" }
 
 func (d *Droid) Run(model string, args []string) error {
+	return fmt.Errorf("droid support is disabled") // DISABLE_DROID
 	if _, err := exec.LookPath("droid"); err != nil {
 		return fmt.Errorf("droid is not installed, install from https://docs.factory.ai/cli/getting-started/quickstart")
 	}
diff '--color=auto' -urp ollama-0.24.0.orig/cmd/launch/hermes.go ollama-0.24.0/cmd/launch/hermes.go
--- ollama-0.24.0.orig/cmd/launch/hermes.go	2026-06-05 01:08:26.394490285 -0700
+++ ollama-0.24.0/cmd/launch/hermes.go	2026-06-05 10:47:40.718243587 -0700
@@ -66,6 +66,8 @@ type Hermes struct{}
 func (h *Hermes) String() string { return "Hermes Agent" }
 
 func (h *Hermes) Run(_ string, args []string) error {
+	return fmt.Errorf("hermes support is disabled") // DISABLE_HERMES
+
 	// Hermes reads its primary model from config.yaml. launch configures that
 	// default model ahead of time so we can keep runtime invocation simple and
 	// still let Hermes discover additional models later via its own UX.
@@ -183,6 +185,8 @@ func (h *Hermes) installed() bool {
 }
 
 func (h *Hermes) ensureInstalled() error {
+	return fmt.Errorf("hermes support is disabled") // DISABLE_HERMES
+
 	if h.installed() {
 		return nil
 	}
@@ -256,6 +260,7 @@ func (h *Hermes) listModels(defaultModel
 }
 
 func (h *Hermes) binary() (string, error) {
+	return "", fmt.Errorf("hermes support is disabled") // DISABLE_HERMES
 	if path, err := hermesLookPath("hermes"); err == nil {
 		return path, nil
 	}
@@ -374,6 +379,7 @@ func (h *Hermes) gatewayRunning() (bool,
 }
 
 func (h *Hermes) gatewayStatusOutput() (string, error) {
+	return "", fmt.Errorf("hermes support is disabled") // DISABLE_HERMES
 	bin, err := h.binary()
 	if err != nil {
 		return "", err
@@ -383,6 +389,7 @@ func (h *Hermes) gatewayStatusOutput() (
 }
 
 func (h *Hermes) restartGateway() error {
+	return fmt.Errorf("hermes support is disabled") // DISABLE_HERMES
 	bin, err := h.binary()
 	if err != nil {
 		return err
diff '--color=auto' -urp ollama-0.24.0.orig/cmd/launch/kimi.go ollama-0.24.0/cmd/launch/kimi.go
--- ollama-0.24.0.orig/cmd/launch/kimi.go	2026-06-05 01:08:26.422489425 -0700
+++ ollama-0.24.0/cmd/launch/kimi.go	2026-06-05 10:49:12.327423929 -0700
@@ -37,6 +37,7 @@ func (k *Kimi) args(config string, extra
 }
 
 func (k *Kimi) Run(model string, args []string) error {
+	return fmt.Errorf("kimi support is disabled") // DISABLE_KIMI
 	if strings.TrimSpace(model) == "" {
 		return fmt.Errorf("model is required")
 	}
@@ -62,6 +63,7 @@ func (k *Kimi) Run(model string, args []
 }
 
 func findKimiBinary() (string, error) {
+	return "", fmt.Errorf("kimi support is disabled") // DISABLE_KIMI
 	if path, err := exec.LookPath("kimi"); err == nil {
 		return path, nil
 	}
@@ -234,6 +236,7 @@ func modelInfoContextLength(modelInfo ma
 }
 
 func ensureKimiInstalled() (string, error) {
+	return "", fmt.Errorf("kimi support is disabled") // DISABLE_KIMI
 	if path, err := findKimiBinary(); err == nil {
 		return path, nil
 	}
diff '--color=auto' -urp ollama-0.24.0.orig/cmd/launch/models.go ollama-0.24.0/cmd/launch/models.go
--- ollama-0.24.0.orig/cmd/launch/models.go	2026-05-14 12:08:13.000000000 -0700
+++ ollama-0.24.0/cmd/launch/models.go	2026-06-05 10:38:05.131957965 -0700
@@ -155,6 +155,7 @@ const (
 
 // OpenBrowser opens the URL in the user's browser.
 func OpenBrowser(url string) {
+	return // DISABLE_CLOUD
 	switch runtime.GOOS {
 	case "darwin":
 		_ = exec.Command("open", url).Start()
@@ -184,6 +185,7 @@ func ensureAuth(ctx context.Context, cli
 }
 
 func ensureCloudAuth(ctx context.Context, client *api.Client, modelList string) error {
+	return errors.New(internalcloud.DisabledError("cloud support is disabled")) // DISABLE_CLOUD
 	if disabled, known := cloudStatusDisabled(ctx, client); known && disabled {
 		return errors.New(internalcloud.DisabledError("remote inference is unavailable"))
 	}
@@ -265,6 +267,7 @@ func showOrPullWithPolicy(ctx context.Co
 	}
 
 	if isCloudModel {
+		return errors.New(internalcloud.DisabledError("cloud support is disabled")) // DISABLE_CLOUD
 		if disabled, known := cloudStatusDisabled(ctx, client); known && disabled {
 			return errors.New(internalcloud.DisabledError("remote inference is unavailable"))
 		}
@@ -523,6 +526,7 @@ func isCloudModel(ctx context.Context, c
 
 // cloudStatusDisabled returns whether cloud usage is currently disabled.
 func cloudStatusDisabled(ctx context.Context, client *api.Client) (disabled bool, known bool) {
+	return status.Cloud.Disabled, true // DISABLE_CLOUD
 	status, err := client.CloudStatusExperimental(ctx)
 	if err != nil {
 		var statusErr api.StatusError
diff '--color=auto' -urp ollama-0.24.0.orig/cmd/launch/openclaw.go ollama-0.24.0/cmd/launch/openclaw.go
--- ollama-0.24.0.orig/cmd/launch/openclaw.go	2026-06-05 01:08:26.422489425 -0700
+++ ollama-0.24.0/cmd/launch/openclaw.go	2026-06-05 10:50:24.117214245 -0700
@@ -35,6 +35,8 @@ type Openclaw struct{}
 func (c *Openclaw) String() string { return "OpenClaw" }
 
 func (c *Openclaw) Run(model string, args []string) error {
+	return fmt.Errorf("OpenClaw support is disabled") // DISABLE_OPENCLAW
+
 	bin, err := ensureOpenclawInstalled()
 	if err != nil {
 		return err
@@ -596,6 +598,7 @@ func canInstallDaemon() bool {
 }
 
 func ensureOpenclawInstalled() (string, error) {
+	return "", fmt.Errorf("OpenClaw support is disabled") // DISABLE_OPENCLAW
 	if _, err := exec.LookPath("openclaw"); err == nil {
 		return "openclaw", nil
 	}
diff '--color=auto' -urp ollama-0.24.0.orig/cmd/launch/opencode.go ollama-0.24.0/cmd/launch/opencode.go
--- ollama-0.24.0.orig/cmd/launch/opencode.go	2026-05-14 12:08:13.000000000 -0700
+++ ollama-0.24.0/cmd/launch/opencode.go	2026-06-05 01:36:00.003710622 -0700
@@ -31,6 +31,7 @@ func (o *OpenCode) String() string { ret
 // findOpenCode returns the opencode binary path, checking PATH first then the
 // curl installer location (~/.opencode/bin) which may not be on PATH yet.
 func findOpenCode() (string, bool) {
+	return "", false // DISABLE_OPENCODE
 	if p, err := exec.LookPath("opencode"); err == nil {
 		return p, true
 	}
@@ -50,6 +51,7 @@ func findOpenCode() (string, bool) {
 }
 
 func (o *OpenCode) Run(model string, args []string) error {
+	return fmt.Errorf("opencode support is disabled") // DISABLE_OPENCODE
 	opencodePath, ok := findOpenCode()
 	if !ok {
 		return fmt.Errorf("opencode is not installed, install from https://opencode.ai")
diff '--color=auto' -urp ollama-0.24.0.orig/cmd/launch/pi.go ollama-0.24.0/cmd/launch/pi.go
--- ollama-0.24.0.orig/cmd/launch/pi.go	2026-06-05 01:08:26.422489425 -0700
+++ ollama-0.24.0/cmd/launch/pi.go	2026-06-05 10:51:32.627105476 -0700
@@ -29,6 +29,7 @@ const (
 func (p *Pi) String() string { return "Pi" }
 
 func (p *Pi) Run(model string, args []string) error {
+	return fmt.Errorf("Pi support is disabled") // DISABLE_PI
 	fmt.Fprintf(os.Stderr, "\n%sPreparing Pi...%s\n", ansiGray, ansiReset)
 	if err := ensureNpmInstalled(); err != nil {
 		return err
@@ -52,6 +53,7 @@ func (p *Pi) Run(model string, args []st
 }
 
 func ensureNpmInstalled() error {
+	return fmt.Errorf("Pi support is disabled") // DISABLE_PI
 	if _, err := exec.LookPath("npm"); err != nil {
 		return fmt.Errorf("npm (Node.js) is required to launch pi\n\nInstall it first:\n  https://nodejs.org/\n\nThen re-run:\n  ollama launch pi")
 	}
@@ -59,6 +61,7 @@ func ensureNpmInstalled() error {
 }
 
 func ensurePiInstalled() (string, error) {
+	return "", fmt.Errorf("Pi support is disabled") // DISABLE_PI
 	if _, err := exec.LookPath("pi"); err == nil {
 		return "pi", nil
 	}
diff '--color=auto' -urp ollama-0.24.0.orig/cmd/launch/poolside.go ollama-0.24.0/cmd/launch/poolside.go
--- ollama-0.24.0.orig/cmd/launch/poolside.go	2026-05-14 12:08:13.000000000 -0700
+++ ollama-0.24.0/cmd/launch/poolside.go	2026-06-05 10:50:45.524555321 -0700
@@ -30,6 +30,8 @@ func (p *Poolside) args(model string, ex
 }
 
 func (p *Poolside) Run(model string, args []string) error {
+	return fmt.Errorf("pool support is disabled") // DISABLE_POOL
+
 	if poolsideGOOS == "windows" {
 		return poolsideUnsupportedError()
 	}
diff '--color=auto' -urp ollama-0.24.0.orig/cmd/launch/vscode.go ollama-0.24.0/cmd/launch/vscode.go
--- ollama-0.24.0.orig/cmd/launch/vscode.go	2026-05-14 12:08:13.000000000 -0700
+++ ollama-0.24.0/cmd/launch/vscode.go	2026-06-05 11:12:01.901261835 -0700
@@ -27,6 +27,7 @@ func (v *VSCode) String() string { retur
 // findBinary returns the path/command to launch VS Code, or "" if not found.
 // It checks platform-specific locations only.
 func (v *VSCode) findBinary() string {
+	return "" // DISABLE_VSCODE
 	var candidates []string
 	switch runtime.GOOS {
 	case "darwin":
@@ -127,6 +128,7 @@ const (
 )
 
 func (v *VSCode) Run(model string, args []string) error {
+	return fmt.Errorf("vscode integration disabled") // DISABLE_VSCODE
 	v.checkVSCodeVersion()
 	v.checkCopilotChatVersion()
 
@@ -214,6 +216,7 @@ func (v *VSCode) ensureModelsRegistered(
 
 // FocusVSCode brings VS Code to the foreground.
 func (v *VSCode) FocusVSCode() {
+	return // DISABLE_VSCODE
 	binary := v.findBinary()
 	if binary == "" {
 		return
@@ -473,6 +476,7 @@ func (v *VSCode) vscodePath(parts ...str
 
 // checkVSCodeVersion warns if VS Code is older than minVSCodeVersion.
 func (v *VSCode) checkVSCodeVersion() {
+	return // DISABLE_VSCODE
 	codeCLI := v.findCodeCLI()
 	if codeCLI == "" {
 		return
@@ -499,6 +503,7 @@ func (v *VSCode) checkVSCodeVersion() {
 // checkCopilotChatVersion warns if the GitHub Copilot Chat extension is
 // missing or older than minCopilotChatVersion.
 func (v *VSCode) checkCopilotChatVersion() {
+	return // DISABLE_VSCODE
 	codeCLI := v.findCodeCLI()
 	if codeCLI == "" {
 		return
@@ -525,6 +530,7 @@ func (v *VSCode) checkCopilotChatVersion
 // On macOS, findBinary may return an .app bundle which can't run --list-extensions,
 // so this resolves to the actual CLI binary inside the bundle.
 func (v *VSCode) findCodeCLI() string {
+	return "" // DISABLE_VSCODE
 	binary := v.findBinary()
 	if binary == "" {
 		return ""
diff '--color=auto' -urp ollama-0.24.0.orig/x/cmd/run.go ollama-0.24.0/x/cmd/run.go
--- ollama-0.24.0.orig/x/cmd/run.go	2026-05-14 12:08:13.000000000 -0700
+++ ollama-0.24.0/x/cmd/run.go	2026-06-05 10:35:04.961502275 -0700
@@ -66,6 +66,7 @@ func isLocalServer() bool {
 }
 
 func cloudStatusDisabled(ctx context.Context, client *api.Client) (disabled bool, known bool) {
+	return status.Cloud.Disabled, true // DISABLE_CLOUD
 	status, err := client.CloudStatusExperimental(ctx)
 	if err != nil {
 		var statusErr api.StatusError
@@ -101,6 +102,8 @@ func waitForOllamaSignin(ctx context.Con
 		return err
 	}
 
+	return errors.New(internalcloud.DisabledError("cloud support is disabled")) // DISABLE_CLOUD
+
 	if disabled, known := cloudStatusDisabled(ctx, client); known && disabled {
 		return errors.New(internalcloud.DisabledError("cloud account endpoints are unavailable"))
 	}
@@ -684,7 +687,10 @@ func GenerateInteractive(cmd *cobra.Comm
 	}
 
 	if enableWebsearch {
-		if client, err := api.ClientFromEnvironment(); err == nil {
+		if @DISABLE_CLOUD_SUPPORT@ {
+			mt.Fprintf(os.Stderr, "%s\n", internalcloud.DisabledError("cloud support is disabled"))
+			enableWebsearch = false
+		} else if client, err := api.ClientFromEnvironment(); err == nil {
 			if disabled, known := cloudStatusDisabled(cmd.Context(), client); known && disabled {
 				fmt.Fprintf(os.Stderr, "%s\n", internalcloud.DisabledError("web search is unavailable"))
 				enableWebsearch = false
diff '--color=auto' -urp ollama-0.24.0.orig/x/imagegen/server.go ollama-0.24.0/x/imagegen/server.go
--- ollama-0.24.0.orig/x/imagegen/server.go	2026-05-14 12:08:13.000000000 -0700
+++ ollama-0.24.0/x/imagegen/server.go	2026-06-05 01:45:53.425485731 -0700
@@ -103,6 +103,8 @@ func (s *Server) Load(ctx context.Contex
 	}
 	s.port = port
 
+	return nil, fmt.Errorf("mlx engine support is disabled"); // DISABLE_MLX
+
 	// Get the current executable path (we use the same binary with runner subcommand)
 	exe, err := os.Executable()
 	if err != nil {
diff '--color=auto' -urp ollama-0.24.0.orig/x/mlxrunner/client.go ollama-0.24.0/x/mlxrunner/client.go
--- ollama-0.24.0.orig/x/mlxrunner/client.go	2026-05-14 12:08:13.000000000 -0700
+++ ollama-0.24.0/x/mlxrunner/client.go	2026-06-05 01:45:57.953346666 -0700
@@ -279,6 +279,8 @@ func (c *Client) Load(ctx context.Contex
 	}
 	c.port = port
 
+	return nil, fmt.Errorf("mlx engine support is disabled"); // DISABLE_MLX
+
 	// Get the current executable path
 	exe, err := os.Executable()
 	if err != nil {
diff '--color=auto' -urp ollama-0.24.0.orig/x/tools/bash.go ollama-0.24.0/x/tools/bash.go
--- ollama-0.24.0.orig/x/tools/bash.go	2026-05-14 12:08:13.000000000 -0700
+++ ollama-0.24.0/x/tools/bash.go	2026-06-05 11:00:08.451226962 -0700
@@ -51,6 +51,7 @@ func (b *BashTool) Schema() api.ToolFunc
 
 // Execute runs the bash command.
 func (b *BashTool) Execute(args map[string]any) (string, error) {
+	return "", fmt.Errorf("bash support disabled") // DISABLE_BASH
 	command, ok := args["command"].(string)
 	if !ok || command == "" {
 		return "", fmt.Errorf("command parameter is required")
