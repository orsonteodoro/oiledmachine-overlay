--- a/cmake/FindHIP/run_hipcc.cmake.orig	2025-12-09 05:30:45.774876656 -0800
+++ b/cmake/FindHIP/run_hipcc.cmake	2025-12-09 05:32:15.516638530 -0800
@@ -28,8 +28,8 @@
 #
 # Input variables:
 #
-# verbose:BOOL=<>               OFF: Be as quiet as possible (default)
-#                               ON : Describe each step
+# verbose:BOOL=<>               OFF: Be as quiet as possible
+#                               ON : Describe each step (default)
 # build_configuration:STRING=<> Build configuration. Defaults to Debug.
 # generated_file:STRING=<>      File to generate. Mandatory argument.
 
@@ -40,6 +40,17 @@ if(NOT generated_file)
     message(FATAL_ERROR "You must specify generated_file on the command line")
 endif()
 
+if(DEFINED ENV{FEATURES} AND "$ENV{FEATURES}" MATCHES "ccache")
+  find_program(CCACHE_PROGRAM ccache)
+  if(CCACHE_PROGRAM)
+    set(HIP_HIPCC_EXECUTABLE "${CCACHE_PROGRAM} ${HIP_HIPCC_EXECUTABLE}")
+    message(STATUS "HIP: ccache enabled → ${HIP_HIPCC_EXECUTABLE}")
+  endif()
+endif()
+
+# Verbose mode – print every command before executing
+set(verbose ON CACHE BOOL "Force verbose HIP compilation" FORCE)
+
 # Set these up as variables to make reading the generated file easier
 set(HIP_HIPCC_EXECUTABLE "@HIP_HIPCC_EXECUTABLE@") # path
 set(HIP_HIPCONFIG_EXECUTABLE "@HIP_HIPCONFIG_EXECUTABLE@") #path
