diff '--color=auto' -urp Tensile-rocm-6.4.4.orig/Tensile/SolutionStructs.py Tensile-rocm-6.4.4/Tensile/SolutionStructs.py
--- Tensile-rocm-6.4.4.orig/Tensile/SolutionStructs.py	2025-08-11 08:14:33.000000000 -0700
+++ Tensile-rocm-6.4.4/Tensile/SolutionStructs.py	2025-11-21 16:04:34.779635117 -0800
@@ -4848,7 +4848,15 @@ class Solution(collections.abc.Mapping):
     maxObjs = 1
     for paramName in data:
       if not isinstance(data[paramName][0],dict):
-        data[paramName] = sorted(data[paramName])
+        tmp = []
+        for v in data[paramName]:
+            if isinstance(v, bool):
+                tmp.append("True" if v else "False")
+            elif isinstance(v, int):
+                tmp.append(str(v))
+            else:
+                tmp.append(v)
+        data[paramName] = sorted(tmp)
       maxObjs *= len(data[paramName])
     numDigits = len(str(maxObjs))
     return [ data, numDigits ]
