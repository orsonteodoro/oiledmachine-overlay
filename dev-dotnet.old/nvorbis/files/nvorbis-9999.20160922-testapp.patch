diff -urp nvorbis-9999.20160922.orig/TestApp/TestApp.csproj nvorbis-9999.20160922/TestApp/TestApp.csproj
--- nvorbis-9999.20160922.orig/TestApp/TestApp.csproj	2017-01-03 17:26:02.689534226 -0800
+++ nvorbis-9999.20160922/TestApp/TestApp.csproj	2017-01-03 17:27:25.631688614 -0800
@@ -102,12 +102,12 @@
     <None Include="packages.config" />
   </ItemGroup>
   <Import Project="$(MSBuildToolsPath)\Microsoft.CSharp.targets" />
-  <Import Project="$(SolutionDir)\.nuget\nuget.targets" />
-  <!-- To modify your build process, add your task inside one of the targets below and uncomment it. 
-       Other similar extension points exist, see Microsoft.Common.targets.
-  <Target Name="BeforeBuild">
-  </Target>
-  <Target Name="AfterBuild">
-  </Target>
+  <Import Project="$(SolutionDir)\.nuget\NuGet.targets" />
+  <!-- To modify your build process, add your task inside one of the targets below and uncomment it. 
+       Other similar extension points exist, see Microsoft.Common.targets.
+  <Target Name="BeforeBuild">
+  </Target>
+  <Target Name="AfterBuild">
+  </Target>
   -->
-</Project>
\ No newline at end of file
+</Project>
