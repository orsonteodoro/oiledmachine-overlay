diff '--color=auto' -urp rapidjson-1.1.0.orig/include/rapidjson/document.h rapidjson-1.1.0/include/rapidjson/document.h
--- rapidjson-1.1.0.orig/include/rapidjson/document.h	2016-08-25 00:59:05.000000000 -0700
+++ rapidjson-1.1.0/include/rapidjson/document.h	2025-11-07 12:41:58.353812306 -0800
@@ -316,7 +316,14 @@ struct GenericStringRef {
 
     GenericStringRef(const GenericStringRef& rhs) : s(rhs.s), length(rhs.length) {}
 
+#ifdef __GNUC__
+    #if defined(__GNUC__) && (__GNUC__ > 14 || (__GNUC__ == 14 && __GNUC_MINOR__ >= 0))
+    #else
+    GenericStringRef& operator=(const GenericStringRef&) = default;
+    #endif
+#else
     GenericStringRef& operator=(const GenericStringRef& rhs) { s = rhs.s; length = rhs.length; }
+#endif
 
     //! implicit conversion to plain CharType pointer
     operator const Ch *() const { return s; }
