<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<!--originator-->
	<maintainer type="person">
		<email>mschiff@gentoo.org</email>
	</maintainer>
	<use>
		<flag name="system-expat">Use the system's Expat library
		provided by the system instead of the internal bundled
		version.</flag>
		<flag name="system-ffmpeg">Use the system's FFmpeg instead of
		the internal bundled version.</flag>
		<flag name="system-icu">Use the system's ICU library instead
		of the internal bundled version.</flag>
		<flag name="system-gdal">Use the system's GDAL library instead
		of the internal bundled version.</flag>
		<flag name="system-openssl">Use the system's OpenSSL instead
		of the internal bundled version.</flag>
		<flag name="system-qt5">Use the system's Qt5 libraries
		instead of the internal bundled version.</flag>
		<flag name="system-spnav">Use the system's spnave libraries
		instead of the internal bundled version.</flag>
	</use>
</pkgmetadata>
