--- a/src/ts/tokenizer.ts.orig	2026-04-18 06:16:46.000000000 -0700
+++ b/src/ts/tokenizer.ts	2026-04-28 22:40:52.381434808 -0700
@@ -182,10 +182,13 @@ export async function encode(data:string
 
 type tokenizerType = 'novellist'|'claude'|'novelai'|'llama'|'mistral'|'llama3'|'gemma'|'cohere'|'googleCloud'|'DeepSeek'
 
-let tikParser:Tiktoken = null
+let tikParser: Tiktoken | null = null
 let tokenizersTokenizer:Tokenizer = null
 let tokenizersType:tokenizerType = null
-let lastTikModel = 'cl100k_base'
+let lastTikModel: string | null = 'cl100k_base'
+
+// This ensures WASM is initialized only once
+let initPromise: Promise<void> | null = null;
 
 let googleCloudTokenizedCache = new Map<string, number>()
 
@@ -235,32 +238,36 @@ async function gemmaTokenize(text:string
     return gemmaTokenizer.encode(text)
 }
 
+async function ensureInitialized() {
+    if (initPromise) return initPromise;
+
+    initPromise = (async () => {
+        // Force proper WASM initialization
+        const { init } = await import('@dqbd/tiktoken/init');
+        await init();                    // ← This is important
+    })();
+
+    return initPromise;
+}
+
 async function tikJS(text:string, model='cl100k_base') {
+    await ensureInitialized();
+
     if(!tikParser || lastTikModel !== model){
-        tikParser?.free()
-        if(model === 'cl100k_base'){
-            const {Tiktoken} = await import('@dqbd/tiktoken')
-            const cl100k_base = await import("@dqbd/tiktoken/encoders/cl100k_base.json");
-            lastTikModel = model   
-        
-            tikParser = new Tiktoken(
-                cl100k_base.bpe_ranks,
-                cl100k_base.special_tokens,
-                cl100k_base.pat_str
-            );
-        }
-        if(model === 'o200k_base'){
-            const {Tiktoken} = await import('@dqbd/tiktoken')
-            const o200k_base = await import("src/etc/o200k_base.json");
-            lastTikModel = model
-            tikParser = new Tiktoken(
-                o200k_base.bpe_ranks,
-                o200k_base.special_tokens,
-                o200k_base.pat_str
-            );
-        }
+        if (tikParser) tikParser.free();
+
+        const { get_encoding } = await import('@dqbd/tiktoken');
+
+        currentModel = model;
+        tikParser = get_encoding(model);   // 'cl100k_base' or 'o200k_base'
     }
-    return tikParser.encode(text)
+    return tikParser.encode(text);
+}
+
+// Optional: cleanup on app unload
+export function freeTiktoken() {
+    tikParser?.free();
+    tikParser = null;
 }
 
 async function geminiTokenizer(text:string) {
--- a/vite.config.ts.orig	2026-04-28 22:10:34.516260008 -0700
+++ b/vite.config.ts	2026-04-28 22:11:22.371441666 -0700
@@ -1,6 +1,7 @@
 import { defineConfig } from "vite";
 import { svelte, vitePreprocess } from "@sveltejs/vite-plugin-svelte";
 import wasm from "vite-plugin-wasm";
+import topLevelAwait from 'vite-plugin-top-level-await';
 import strip from '@rollup/plugin-strip';
 import tailwindcss from '@tailwindcss/vite'
 // https://vitejs.dev/config/
@@ -17,6 +18,7 @@ export default defineConfig(({command, m
       }),
       tailwindcss(),
       wasm(),
+      topLevelAwait(),
       command === 'build' ? strip({
         include: '**/*.(mjs|js|svelte|ts)'
       }) : null
@@ -36,7 +38,7 @@ export default defineConfig(({command, m
     // https://v2.tauri.app/reference/environment-variables/
     envPrefix: ["VITE_", "TAURI_"],
     build: {
-      target:'baseline-widely-available',
+      target:'es2020', 
       // don't minify for debug builds
       minify: process.env.TAURI_ENV_DEBUG === 'true' ? false : 'oxc',
       // produce sourcemaps for debug builds
