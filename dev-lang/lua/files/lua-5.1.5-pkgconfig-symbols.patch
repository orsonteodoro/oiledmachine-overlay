diff '--color=auto' -urp lua-5.1.5.orig/etc/lua.pc lua-5.1.5/etc/lua.pc
--- lua-5.1.5.orig/etc/lua.pc	2012-02-10 01:50:30.000000000 -0800
+++ lua-5.1.5/etc/lua.pc	2026-05-19 16:42:18.279566568 -0700
@@ -8,24 +8,24 @@ V= 5.1
 R= 5.1.5
 
 # grep '^INSTALL_.*=' ../Makefile | sed 's/INSTALL_TOP/prefix/'
-prefix= /usr/local
+prefix= @STAGING_PREFIX@
 INSTALL_BIN= ${prefix}/bin
-INSTALL_INC= ${prefix}/include
-INSTALL_LIB= ${prefix}/lib
-INSTALL_MAN= ${prefix}/man/man1
-INSTALL_LMOD= ${prefix}/share/lua/${V}
-INSTALL_CMOD= ${prefix}/lib/lua/${V}
+INSTALL_INC= ${prefix}/include/lua@LUA_SLOT@
+INSTALL_LIB= ${prefix}/@LIBDIR@
+INSTALL_MAN= ${prefix}/share/man/man1
+INSTALL_LMOD= ${prefix}/share/lua/@LUA_SLOT@
+INSTALL_CMOD= ${prefix}/@LIBDIR@/lua/@LUA_SLOT@
 
 # canonical vars
 exec_prefix=${prefix}
-libdir=${exec_prefix}/lib
-includedir=${prefix}/include
+libdir=${exec_prefix}/@LIBDIR@
+includedir=${prefix}/include/lua@LUA_SLOT@
 
 Name: Lua
 Description: An Extensible Extension Language
 Version: ${R}
 Requires: 
-Libs: -L${libdir} -llua -lm
+Libs: -L${libdir} -llua@LUA_SLOT@ -lm
 Cflags: -I${includedir}
 
 # (end of lua.pc)
