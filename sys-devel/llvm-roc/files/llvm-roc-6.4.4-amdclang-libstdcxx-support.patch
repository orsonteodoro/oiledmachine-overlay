--- a/CMakeLists.txt.orig	2025-06-05 10:11:31.000000000 -0700
+++ b/CMakeLists.txt	2025-12-04 20:48:01.970681222 -0800
@@ -1,35 +1,40 @@
-if(UNIX AND LIBCXX_ENABLE_STATIC AND LIBCXXABI_ENABLE_STATIC)
+if(UNIX)
   add_clang_tool(amdllvm
     amdllvm.cpp
-
-    DEPENDS
-    clang
-    runtimes
-    lld
+    DEPENDS clang lld
   )
 
-  # This tool has a dependency on C++17 filesystem library;
-  # however, some Linux distros still don't have sufficient support for C++17.
-  # Compile with clang and static link to libc++ to workaround this issue.
-  set(CMAKE_CXX_COMPILER ${CMAKE_BINARY_DIR}/bin/clang++)
-  set(CMAKE_CXX_LINKER ${CMAKE_CXX_COMPILER})
   target_compile_features(amdllvm PRIVATE cxx_std_17)
-  target_compile_options(amdllvm PRIVATE  -nostdinc++)
-  target_include_directories(amdllvm PRIVATE ${CMAKE_BINARY_DIR}/include/c++/v1)
-  target_include_directories(amdllvm PRIVATE ${CMAKE_BINARY_DIR}/include/${LLVM_DEFAULT_TARGET_TRIPLE}/c++/v1)
-  target_link_libraries(amdllvm PRIVATE c++abi pthread)
-  target_link_options(amdllvm PRIVATE -stdlib=libc++
-                                      -L${CMAKE_BINARY_DIR}/lib
-                                      -fuse-ld=lld
+
+  # Use the normal system libstdc++ by default (this is what every ROCm tool does)
+  target_link_libraries(amdllvm PRIVATE stdc++ pthread)
+
+  target_link_options(amdllvm PRIVATE
+    -fuse-ld=lld
+    -rtlib=compiler-rt          # avoid libgcc_s dependency
+    --hip-link                   # required for HIP device linking when amdllvm is used as hipcc wrapper
   )
-  option(CLANG_LINK_FLANG "Create flang install link to clang" ON)
-  if(NOT CLANG_LINKS_TO_CREATE)
-    if(CLANG_LINK_FLANG)
-      set(CLANG_LINKS_TO_CREATE clang clang++ clang-cl clang-cpp clang-${CLANG_VERSION_MAJOR} flang
-                              lld)
-    else()
-      set(CLANG_LINKS_TO_CREATE clang clang++ clang-cl clang-cpp clang-${CLANG_VERSION_MAJOR} lld)
+
+  # Optional: allow forcing libc++ (only for developers who really want it)
+  option(AMDLLVM_USE_LIBCXX "Force amdllvm to use the just-built libc++ (not recommended)" OFF)
+  if(AMDLLVM_USE_LIBCXX)
+    if(NOT TARGET c++ OR NOT TARGET c++abi)
+      message(FATAL_ERROR "libc++ and libc++abi runtimes must be built for AMDLLVM_USE_LIBCXX=ON")
     endif()
+    target_compile_options(amdllvm PRIVATE -nostdinc++)
+    target_include_directories(amdllvm PRIVATE
+      ${LLVM_BINARY_DIR}/include/c++/v1
+    )
+    target_link_libraries(amdllvm PRIVATE c++ c++abi) # Overrides stdc++
+    target_link_options(amdllvm PRIVATE -stdlib=libc++)
+  endif()
+
+  # Create the amdclang, amdclang++, amdflang, ... symlinks
+  set(CLANG_LINKS_TO_CREATE clang clang++ clang-cl clang-cpp clang-${CLANG_VERSION_MAJOR} lld)
+
+  option(CLANG_LINK_FLANG "Also create amdflang → amdllvm symlink" ON)
+  if(CLANG_LINK_FLANG)
+    list(APPEND CLANG_LINKS_TO_CREATE flang)
   endif()
 
   foreach(link ${CLANG_LINKS_TO_CREATE})
