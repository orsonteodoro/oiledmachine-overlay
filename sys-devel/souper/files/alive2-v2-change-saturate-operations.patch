Subject:  Change operations for 32-bit ABI
Patch status:  Testing
--- a/third_party/alive2/util/compiler.cpp.orig	2021-05-01 11:08:33.000000000 -0700
+++ b/third_party/alive2/util/compiler.cpp	2022-03-21 23:36:53.811744655 -0700
@@ -44,15 +44,13 @@ unsigned num_sign_bits(uint64_t n) {
 }
 
 uint64_t add_saturate(uint64_t a, uint64_t b) {
-  unsigned long res;
-  static_assert(sizeof(res) == sizeof(uint64_t));
-  return __builtin_uaddl_overflow(a, b, &res) ? UINT64_MAX : res;
+  uint64_t res;
+  return __builtin_add_overflow(a, b, &res) ? UINT64_MAX : res;
 }
 
 uint64_t mul_saturate(uint64_t a, uint64_t b) {
-  unsigned long res;
-  static_assert(sizeof(res) == sizeof(uint64_t));
-  return __builtin_umull_overflow(a, b, &res) ? UINT64_MAX : res;
+  uint64_t res;
+  return __builtin_mul_overflow(a, b, &res) ? UINT64_MAX : res;
 }
 
 uint64_t divide_up(uint64_t n, uint64_t amount) {
