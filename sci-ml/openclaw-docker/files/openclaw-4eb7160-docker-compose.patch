--- a/docker-compose.yml.orig	2026-04-09 22:29:11.919446270 -0700
+++ b/docker-compose.yml	2026-04-09 22:30:19.235191365 -0700
@@ -1,6 +1,7 @@
 services:
   openclaw-gateway:
-    image: ${OPENCLAW_IMAGE:-openclaw:local}
+    image: ghcr.io/openclaw/openclaw:@OPENCLAW_TAG@
+    user: "@OPENCLAW_UID@:@OPENCLAW_GID@"
     environment:
       HOME: /home/node
       TERM: xterm-256color
@@ -11,7 +12,7 @@ services:
       CLAUDE_WEB_COOKIE: ${CLAUDE_WEB_COOKIE:-}
       TZ: ${OPENCLAW_TZ:-UTC}
     volumes:
-      - ${OPENCLAW_CONFIG_DIR}:/home/node/.openclaw
+      - /var/lib/openclaw:/home/node/.openclaw
       - ${OPENCLAW_WORKSPACE_DIR}:/home/node/.openclaw/workspace
       ## Uncomment the lines below to enable sandbox isolation
       ## (agents.defaults.sandbox). Requires Docker CLI in the image
