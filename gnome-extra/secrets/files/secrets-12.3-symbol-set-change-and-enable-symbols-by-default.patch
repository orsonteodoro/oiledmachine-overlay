Enable symbols by default and use a conservative symbol set.
diff '--color=auto' -urp secrets-12.3.orig/data/gtk/password_generator_popover.ui secrets-12.3/data/gtk/password_generator_popover.ui
--- secrets-12.3.orig/data/gtk/password_generator_popover.ui	2026-05-07 12:14:54.983473357 -0700
+++ secrets-12.3/data/gtk/password_generator_popover.ui	2026-05-07 12:15:43.293813558 -0700
@@ -76,6 +76,7 @@
                               <object class="GtkToggleButton" id="_special_toggle_button">
                                 <property name="label">#§*</property>
                                 <property name="hexpand">True</property>
+                                <property name="active">True</property>
                               </object>
                             </child>
                             <style>
diff '--color=auto' -urp secrets-12.3.orig/data/org.gnome.World.Secrets.gschema.xml.in secrets-12.3/data/org.gnome.World.Secrets.gschema.xml.in
--- secrets-12.3.orig/data/org.gnome.World.Secrets.gschema.xml.in	2026-05-07 12:14:54.983908439 -0700
+++ secrets-12.3/data/org.gnome.World.Secrets.gschema.xml.in	2026-05-07 12:15:56.941371675 -0700
@@ -88,7 +88,7 @@
             <description>Use numbers 0-9 characters when generating a random password.</description>
         </key>
         <key type="b" name="generator-use-symbols">
-            <default>false</default>
+            <default>true</default>
             <summary>Use symbols when generating a password</summary>
             <description>Use non-alphanumeric ASCII symbols when generating a random password.</description>
         </key>
diff '--color=auto' -urp secrets-12.3.orig/gsecrets/password_generator.py secrets-12.3/gsecrets/password_generator.py
--- secrets-12.3.orig/gsecrets/password_generator.py	2026-02-22 05:59:13.000000000 -0800
+++ secrets-12.3/gsecrets/password_generator.py	2026-05-07 12:58:08.481968444 -0700
@@ -34,7 +34,6 @@ def _satisfies_requirements(
 
     return True
 
-
 def generate(
     length: int,
     use_uppercase: bool,
@@ -63,9 +62,16 @@ def generate(
     if use_numbers:
         characters += string.digits
 
+    # The set is to avoid rejection and symbols that may break websites.
+    SAFE_SYMBOLS = "!@#$%^&*()-_=+"
+
     if use_symbols:
-        characters += string.punctuation
+        if @GSECRETS_SAFE_SYMBOLS@:
+            characters += SAFE_SYMBOLS
+        else:
+            characters += string.punctuation # The original upstream default with '!"#$%&\'()*+,-./:;<=>?@[\\]^_`{|}~'
 
+    # Fallback if nothing selected
     # If all options are disabled, generate a password using
     # arbitrary ASCII characters.
     # TODO Revisit this. It might be a sane default, but
@@ -75,7 +81,7 @@ def generate(
             string.ascii_uppercase
             + string.ascii_lowercase
             + string.digits
-            + string.punctuation
+            + SAFE_SYMBOLS
         )
 
     while True:
