diff '--color=auto' -urp secrets-12.3.orig/data/gtk/database_settings_dialog.ui secrets-12.3/data/gtk/database_settings_dialog.ui
--- secrets-12.3.orig/data/gtk/database_settings_dialog.ui	2026-02-22 05:59:13.000000000 -0800
+++ secrets-12.3/data/gtk/database_settings_dialog.ui	2026-05-05 16:26:37.817137952 -0700
@@ -247,6 +247,7 @@
         </child>
         <child>
           <object class="AdwPreferencesGroup" id="hibp_group">
+            <property name="visible">@HIBP_SUPPORT@</property>
             <property name="title" translatable="yes">Check passwords for online breaches</property>
             <property name="description" translatable="yes">Ask online whether your passwords have been hacked. The passwords won't be sent online! The following passwords have been leaked:</property>
             <property name="header-suffix">
diff '--color=auto' -urp secrets-12.3.orig/gsecrets/widgets/database_settings_dialog.py secrets-12.3/gsecrets/widgets/database_settings_dialog.py
--- secrets-12.3.orig/gsecrets/widgets/database_settings_dialog.py	2026-02-22 05:59:13.000000000 -0800
+++ secrets-12.3/gsecrets/widgets/database_settings_dialog.py	2026-05-05 16:23:47.550212829 -0700
@@ -6,11 +6,19 @@ import threading
 import typing
 from gettext import gettext as _
 from pathlib import Path, PurePath
-
-import pyhibp
-import requests
 from gi.repository import Adw, Gio, GLib, GObject, Gtk
-from pyhibp import pwnedpasswords as pw
+
+HIBP_SUPPORT = @HIBP_SUPPORT@
+
+if HIBP_SUPPORT:
+    import pyhibp
+    import requests
+    from pyhibp import pwnedpasswords as pw
+else:
+    pyhibp = None
+    pw = None
+    import logging
+    logging.getLogger(__name__).debug("pyhibp not available, HIBP checks disabled")
 
 from gsecrets.password_generator import strength
 from gsecrets.utils import (
@@ -148,11 +156,12 @@ class DatabaseSettingsDialog(Adw.Prefere
         }
         self.unique_passwords_row.set_subtitle(str(len(unique_dict)))
 
-        thread = threading.Thread(target=self.check_network_access)
-        thread.daemon = True
-        thread.start()
+        if HIBP_SUPPORT:
+            thread = threading.Thread(target=self.check_network_access)
+            thread.daemon = True
+            thread.start()
 
-        self.hibp_stack.set_visible_child_name("button")
+            self.hibp_stack.set_visible_child_name("button")
 
     def check_network_access(self):
         reachable = True
