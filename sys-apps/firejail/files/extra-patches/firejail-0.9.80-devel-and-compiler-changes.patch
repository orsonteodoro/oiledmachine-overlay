diff '--color=auto' -urp firejail-0.9.80.orig/etc/inc/allow-nodejs.inc firejail-0.9.80/etc/inc/allow-nodejs.inc
--- firejail-0.9.80.orig/etc/inc/allow-nodejs.inc	2026-03-12 05:51:18.000000000 -0700
+++ firejail-0.9.80/etc/inc/allow-nodejs.inc	2026-05-25 17:22:00.716427359 -0700
@@ -6,6 +6,7 @@ ignore read-only ${HOME}/.nvm
 noblacklist ${HOME}/.nvm
 noblacklist ${PATH}/node
 noblacklist /usr/include/node
+noblacklist /usr/lib/node
 
 # Allow python for node-gyp (blacklisted by disable-interpreters.inc)
 include allow-python2.inc
diff '--color=auto' -urp firejail-0.9.80.orig/etc/inc/disable-devel.inc firejail-0.9.80/etc/inc/disable-devel.inc
--- firejail-0.9.80.orig/etc/inc/disable-devel.inc	2026-03-12 05:51:18.000000000 -0700
+++ firejail-0.9.80/etc/inc/disable-devel.inc	2026-05-25 17:22:00.716427359 -0700
@@ -44,6 +44,7 @@ blacklist ${PATH}/llvm*
 blacklist ${PATH}/scan-build
 # see issue #2106 - it disables hardware acceleration in Firefox on Radeon GPU
 #blacklist /usr/lib/llvm*
+blacklist ${PATH}/flang*
 
 # GCC
 blacklist ${PATH}/*-g++*
@@ -66,6 +67,9 @@ blacklist ${PATH}/make-first-existing-ta
 blacklist ${PATH}/x86_64-linux-gnu-*
 # seems to create problems on Gentoo
 #blacklist /usr/lib/gcc
+blacklist ${PATH}/c++filt
+blacklist ${PATH}/ld.gold
+blacklist ${PATH}/ld.bfd
 
 # Go
 blacklist ${PATH}/gccgo
@@ -94,6 +98,7 @@ blacklist ${PATH}/openssl-1.0
 blacklist ${PATH}/rust-gdb
 blacklist ${PATH}/rust-lldb
 blacklist ${PATH}/rustc
+blacklist ${PATH}/cargo
 
 # tcc - Tiny C Compiler
 blacklist ${PATH}/tcc
@@ -109,3 +114,77 @@ blacklist /usr/include
 blacklist /usr/local/include
 blacklist /usr/local/src
 blacklist /usr/src
+
+# npm
+blacklist ${PATH}/npm
+
+# pnpm
+blacklist ${PATH}/pnpm
+
+# yarn
+blacklist ${PATH}/yarn
+
+# Multislot npm
+blacklist /usr/share/npm
+
+# Multislot yarn
+blacklist /usr/share/pnpm
+
+# Multislot yarn
+blacklist /usr/share/yarn
+
+# Bazel
+blacklist ${PATH}/bazel*
+
+# Conan
+blacklist ${PATH}/conan
+blacklist /usr/lib/python*/site-packages/conan
+
+# Mono
+blacklist ${PATH}/csc
+blacklist ${PATH}/gacutil
+blacklist ${PATH}/ilasm
+blacklist ${PATH}/mcs
+blacklist ${PATH}/mkbundle
+blacklist ${PATH}/mono
+blacklist ${PATH}/monodis
+blacklist ${PATH}/sn
+
+# .NET
+blacklist ${PATH}/dotnet
+
+# glibc
+blacklist ${PATH}/lld
+
+# Binutils
+blacklist ${PATH}/objdump
+blacklist ${PATH}/readelf
+blacklist ${PATH}/elfedit
+
+# Profiling
+blacklist ${PATH}/dtrace
+blacklist ${PATH}/ltrace
+blacklist ${PATH}/rr
+blacklist ${PATH}/stap
+blacklist ${PATH}/strace
+blacklist ${PATH}/perf
+blacklist ${PATH}/ptrace
+blacklist ${PATH}/valgrind
+
+# Debuggers
+blacklist ${PATH}/gdb
+
+# Patchelf / chrpath
+blacklist ${PATH}/patchelf
+blacklist ${PATH}/chrpath
+
+# Mold
+blacklist ${PATH}/mold
+
+# Cython
+blacklist ${PATH}/cygdb
+blacklist ${PATH}/cython*
+blacklist ${PATH}/cythonize
+
+# Multislot Cython
+blacklist /usr/lib/cython/*/lib/python*/site-packages/Cython
diff '--color=auto' -urp firejail-0.9.80.orig/etc/inc/disable-interpreters.inc firejail-0.9.80/etc/inc/disable-interpreters.inc
--- firejail-0.9.80.orig/etc/inc/disable-interpreters.inc	2026-03-12 05:51:18.000000000 -0700
+++ firejail-0.9.80/etc/inc/disable-interpreters.inc	2026-05-25 17:22:00.716427359 -0700
@@ -29,6 +29,7 @@ blacklist /usr/lib64/libmozjs-*
 # Node.js
 blacklist ${PATH}/node
 blacklist /usr/include/node
+blacklist /usr/lib/node
 
 # nvm
 blacklist ${HOME}/.nvm
@@ -80,3 +81,6 @@ blacklist /usr/lib/python3*
 blacklist /usr/lib64/python3*
 blacklist /usr/local/lib/python3*
 blacklist /usr/share/python3*
+
+# JSC
+blacklist ${PATH}/jsc
diff '--color=auto' -urp firejail-0.9.80.orig/etc/profile-a-l/epiphany.profile firejail-0.9.80/etc/profile-a-l/epiphany.profile
--- firejail-0.9.80.orig/etc/profile-a-l/epiphany.profile	2026-03-12 05:51:18.000000000 -0700
+++ firejail-0.9.80/etc/profile-a-l/epiphany.profile	2026-05-25 17:23:15.770131212 -0700
@@ -12,6 +12,7 @@ include globals.local
 noblacklist ${HOME}/.cache/epiphany
 noblacklist ${HOME}/.config/epiphany
 noblacklist ${HOME}/.local/share/epiphany
+noblacklist ${PATH}/jsc
 
 include disable-common.inc
 include disable-devel.inc
@@ -25,6 +26,7 @@ whitelist ${DOWNLOADS}
 whitelist ${HOME}/.cache/epiphany
 whitelist ${HOME}/.config/epiphany
 whitelist ${HOME}/.local/share/epiphany
+whitelist ${PATH}/jsc
 include whitelist-common.inc
 
 caps.drop all
diff '--color=auto' -urp firejail-0.9.80.orig/etc/profile-a-l/evolution.profile firejail-0.9.80/etc/profile-a-l/evolution.profile
--- firejail-0.9.80.orig/etc/profile-a-l/evolution.profile	2026-03-12 05:51:18.000000000 -0700
+++ firejail-0.9.80/etc/profile-a-l/evolution.profile	2026-05-25 17:23:03.242514474 -0700
@@ -16,6 +16,7 @@ noblacklist ${HOME}/.gnupg
 noblacklist ${HOME}/.local/share/evolution
 noblacklist ${HOME}/.pki
 noblacklist ${HOME}/.local/share/pki
+noblacklist ${PATH}/jsc
 
 include disable-common.inc
 include disable-devel.inc
@@ -25,6 +26,8 @@ include disable-programs.inc
 
 include whitelist-runuser-common.inc
 
+whitelist ${PATH}/jsc
+
 writable-var
 
 caps.drop all
diff '--color=auto' -urp firejail-0.9.80.orig/etc/profile-a-l/geary.profile firejail-0.9.80/etc/profile-a-l/geary.profile
--- firejail-0.9.80.orig/etc/profile-a-l/geary.profile	2026-03-12 05:51:18.000000000 -0700
+++ firejail-0.9.80/etc/profile-a-l/geary.profile	2026-05-25 17:24:22.328094989 -0700
@@ -16,6 +16,7 @@ noblacklist ${HOME}/.local/share/geary
 noblacklist ${HOME}/.local/share/pki
 noblacklist ${HOME}/.mailcap
 noblacklist ${HOME}/.pki
+noblacklist ${PATH}/jsc
 
 # sh is needed to allow Firefox to open links
 include allow-bin-sh.inc
@@ -35,6 +36,7 @@ noblacklist ${HOME}/.config/mozilla
 noblacklist ${HOME}/.mozilla
 whitelist ${HOME}/.config/mozilla/firefox/profiles.ini
 whitelist ${HOME}/.mozilla/firefox/profiles.ini
+whitelist ${PATH}/jsc
 
 mkdir ${HOME}/.cache/evolution
 mkdir ${HOME}/.cache/folks
diff '--color=auto' -urp firejail-0.9.80.orig/etc/profile-m-z/midori.profile firejail-0.9.80/etc/profile-m-z/midori.profile
--- firejail-0.9.80.orig/etc/profile-m-z/midori.profile	2026-03-12 05:51:18.000000000 -0700
+++ firejail-0.9.80/etc/profile-m-z/midori.profile	2026-05-25 17:24:23.512058768 -0700
@@ -21,6 +21,8 @@ noblacklist ${HOME}/.cache/gnome-mplayer
 noblacklist ${HOME}/.config/gnome-mplayer
 noblacklist ${HOME}/.lastpass
 
+noblacklist ${PATH}/jsc
+
 include disable-common.inc
 include disable-devel.inc
 include disable-exec.inc
@@ -45,6 +47,7 @@ whitelist ${HOME}/.local/share/pki
 whitelist ${HOME}/.local/share/webkit
 whitelist ${HOME}/.local/share/webkitgtk
 whitelist ${HOME}/.pki
+whitelist ${PATH}/jsc
 include whitelist-common.inc
 include whitelist-var-common.inc
 
diff '--color=auto' -urp firejail-0.9.80.orig/etc/profile-m-z/surf.profile firejail-0.9.80/etc/profile-m-z/surf.profile
--- firejail-0.9.80.orig/etc/profile-m-z/surf.profile	2026-03-12 05:51:18.000000000 -0700
+++ firejail-0.9.80/etc/profile-m-z/surf.profile	2026-05-25 17:24:17.752234979 -0700
@@ -7,6 +7,7 @@ include surf.local
 include globals.local
 
 noblacklist ${HOME}/.surf
+noblacklist ${PATH}/jsc
 
 include disable-common.inc
 include disable-devel.inc
@@ -16,6 +17,7 @@ include disable-programs.inc
 mkdir ${HOME}/.surf
 whitelist ${HOME}/.surf
 whitelist ${DOWNLOADS}
+whitelist ${PATH}/jsc
 include whitelist-common.inc
 
 caps.drop all
