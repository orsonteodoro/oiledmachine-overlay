diff '--color=auto' -urp firejail-0.9.80.orig/src/firejail/firejail.h firejail-0.9.80/src/firejail/firejail.h
--- firejail-0.9.80.orig/src/firejail/firejail.h	2026-03-12 05:51:18.000000000 -0700
+++ firejail-0.9.80/src/firejail/firejail.h	2026-04-06 01:28:00.445787555 -0700
@@ -359,6 +359,7 @@ extern bool apparmor_replace; // whether
 extern int arg_allow_debuggers;	// allow debuggers
 extern int arg_x11_block;	// block X11
 extern int arg_x11_xorg;	// use X11 security extension
+extern int arg_xpra_speaker;	// enable sound forwarding
 extern int arg_allusers;	// all user home directories visible
 extern int arg_machineid;	// spoof /etc/machine-id
 extern int arg_disable_mnt;	// disable /mnt and /media
Only in firejail-0.9.80/src/firejail: firejail.h.orig
diff '--color=auto' -urp firejail-0.9.80.orig/src/firejail/main.c firejail-0.9.80/src/firejail/main.c
--- firejail-0.9.80.orig/src/firejail/main.c	2026-03-12 05:51:18.000000000 -0700
+++ firejail-0.9.80/src/firejail/main.c	2026-04-06 01:28:00.446408868 -0700
@@ -146,6 +146,7 @@ bool apparmor_replace = false;	// apparm
 int arg_allow_debuggers = 0;			// allow debuggers
 int arg_x11_block = 0;				// block X11
 int arg_x11_xorg = 0;				// use X11 security extension
+int arg_xpra_speaker = 0;			// enable sound forwarding
 int arg_allusers = 0;				// all user home directories visible
 int arg_machineid = 0;				// spoof /etc/machine-id
 int arg_allow_private_blacklist = 0;		// blacklist things in private directories
@@ -2759,6 +2760,10 @@ int main(int argc, char **argv, char **e
 				exit_err_feature(argv[i], CFG_X11);
 		}
 #endif
+		else if (strcmp(argv[i], "--xpra-speaker=0") == 0)
+			arg_xpra_speaker = 0;
+		else if (strcmp(argv[i], "--xpra-speaker=1") == 0)
+			arg_xpra_speaker = 1;
 		else if (strncmp(argv[i], "--join-or-start=", 16) == 0) {
 			// Note: This is the second part of the option handler;
 			// the attempt to find and join the sandbox is done in
Only in firejail-0.9.80/src/firejail: main.c.orig
diff '--color=auto' -urp firejail-0.9.80.orig/src/firejail/profile.c firejail-0.9.80/src/firejail/profile.c
--- firejail-0.9.80.orig/src/firejail/profile.c	2026-03-12 05:51:18.000000000 -0700
+++ firejail-0.9.80/src/firejail/profile.c	2026-04-06 01:28:00.447041491 -0700
@@ -1425,6 +1425,14 @@ int profile_check_line(char *ptr, int li
 		return 0;
 	}
 
+	if (strcmp(ptr, "xpra_speaker_off") == 0) {
+		arg_xpra_speaker=0;
+	}
+
+	if (strcmp(ptr, "xpra_speaker_on") == 0) {
+		arg_xpra_speaker=1;
+	}
+
 	// private /etc list of files and directories
 	if (strncmp(ptr, "private-etc ", 12) == 0) {
 		if (checkcfg(CFG_PRIVATE_ETC)) {
Only in firejail-0.9.80/src/firejail: profile.c.orig
diff '--color=auto' -urp firejail-0.9.80.orig/src/firejail/x11.c firejail-0.9.80/src/firejail/x11.c
--- firejail-0.9.80.orig/src/firejail/x11.c	2026-03-12 05:51:18.000000000 -0700
+++ firejail-0.9.80/src/firejail/x11.c	2026-04-06 01:28:00.447394129 -0700
@@ -839,11 +839,18 @@ static void __attribute__((noreturn)) x1
 	}
 	free(fname);
 
+	char speaker_str[14];
+	if (arg_xpra_speaker) {
+		strcpy(speaker_str,"--speaker=on");
+	} else {
+		strcpy(speaker_str,"--speaker=off");
+	}
+
 	// build attach command
 
 	char * title_arg_str = get_title_arg_str();
 
-	char *attach_argv[] = { "xpra", title_arg_str, "attach", display_str, NULL };
+	char *attach_argv[] = { "xpra", title_arg_str, "attach", speaker_str, display_str, NULL };
 
 	// run attach command
 	client = fork();
@@ -1082,6 +1089,15 @@ static void __attribute__((noreturn)) x1
 			errExit("open");
 	}
 
+	char speaker_str[2];
+	if (arg_xpra_speaker) {
+		strcpy(speaker_str,"1");
+	} else {
+		strcpy(speaker_str,"0");
+	}
+
+	setenv("XPRA_SPEAKER", speaker_str, 1);
+
 	// start
 	__gcov_flush();
 	server = fork();
Only in firejail-0.9.80/src/firejail: x11.c.orig
