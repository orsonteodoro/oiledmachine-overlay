diff -urp firejail-0.9.60.orig/src/firejail/firejail.h firejail-0.9.60/src/firejail/firejail.h
--- firejail-0.9.60.orig/src/firejail/firejail.h	2019-05-27 05:28:15.000000000 -0700
+++ firejail-0.9.60/src/firejail/firejail.h	2020-05-13 09:57:31.798782886 -0700
@@ -307,6 +307,7 @@ extern int arg_apparmor;	// apparmor
 extern int arg_allow_debuggers;	// allow debuggers
 extern int arg_x11_block;	// block X11
 extern int arg_x11_xorg;	// use X11 security extension
+extern int arg_xpra_speaker;	// enable sound forwarding
 extern int arg_allusers;	// all user home directories visible
 extern int arg_machineid;	// preserve /etc/machine-id
 extern int arg_disable_mnt;	// disable /mnt and /media
Only in firejail-0.9.60/src/firejail: firejail.h.orig
diff -urp firejail-0.9.60.orig/src/firejail/main.c firejail-0.9.60/src/firejail/main.c
--- firejail-0.9.60.orig/src/firejail/main.c	2019-05-27 05:28:15.000000000 -0700
+++ firejail-0.9.60/src/firejail/main.c	2020-05-13 09:57:31.798782886 -0700
@@ -116,6 +116,7 @@ int arg_apparmor = 0;				// apparmor
 int arg_allow_debuggers = 0;			// allow debuggers
 int arg_x11_block = 0;				// block X11
 int arg_x11_xorg = 0;				// use X11 security extension
+int arg_xpra_speaker = 0;			// enable sound forwarding
 int arg_allusers = 0;				// all user home directories visible
 int arg_machineid = 0;				// preserve /etc/machine-id
 int arg_allow_private_blacklist = 0; 		// blacklist things in private directories
@@ -2291,6 +2292,10 @@ int main(int argc, char **argv) {
 				exit_err_feature("x11");
 		}
 #endif
+		else if (strcmp(argv[i], "--xpra-speaker=0") == 0)
+			arg_xpra_speaker = 0;
+		else if (strcmp(argv[i], "--xpra-speaker=1") == 0)
+			arg_xpra_speaker = 1;
 		else if (strncmp(argv[i], "--join-or-start=", 16) == 0) {
 			// NOTE: this is second part of option handler,
 			//		 atempt to find and join sandbox is done in other one
Only in firejail-0.9.60/src/firejail: main.c.orig
diff -urp firejail-0.9.60.orig/src/firejail/profile.c firejail-0.9.60/src/firejail/profile.c
--- firejail-0.9.60.orig/src/firejail/profile.c	2019-05-27 05:28:15.000000000 -0700
+++ firejail-0.9.60/src/firejail/profile.c	2020-05-13 09:57:31.798782886 -0700
@@ -1032,6 +1032,14 @@ int profile_check_line(char *ptr, int li
 		return 0;
 	}
 
+	if (strcmp(ptr, "xpra_speaker_off") == 0) {
+		arg_xpra_speaker=0;
+	}
+
+	if (strcmp(ptr, "xpra_speaker_on") == 0) {
+		arg_xpra_speaker=1;
+	}
+
 	// private /etc list of files and directories
 	if (strncmp(ptr, "private-etc ", 12) == 0) {
 		if (arg_writable_etc) {
Only in firejail-0.9.60/src/firejail: profile.c.orig
diff -urp firejail-0.9.60.orig/src/firejail/x11.c firejail-0.9.60/src/firejail/x11.c
--- firejail-0.9.60.orig/src/firejail/x11.c	2019-05-27 05:28:15.000000000 -0700
+++ firejail-0.9.60/src/firejail/x11.c	2020-05-13 10:01:00.607274293 -0700
@@ -749,8 +749,15 @@ void x11_start_xpra_old(int argc, char *
 	}
 	free(fname);
 
+	char speaker_str[14];
+	if (arg_xpra_speaker) {
+		strcpy(speaker_str,"--speaker=on");
+	} else {
+		strcpy(speaker_str,"--speaker=off");
+	}
+
 	// build attach command
-	char *attach_argv[] = { "xpra", "--title=\"firejail x11 sandbox\"", "attach", display_str, NULL };
+	char *attach_argv[] = { "xpra", "--title=\"firejail x11 sandbox\"", "attach", speaker_str, display_str, NULL };
 
 	// run attach command
 	client = fork();
@@ -989,6 +996,15 @@ void x11_start_xpra_new(int argc, char *
 			dup2(fd_null,2);
 		}
 
+		char speaker_str[2];
+		if (arg_xpra_speaker) {
+			strcpy(speaker_str,"1");
+		} else {
+			strcpy(speaker_str,"0");
+		}
+
+		setenv("XPRA_SPEAKER", speaker_str, 1);
+
 		// running without privileges - see drop_privs call above
 		assert(getenv("LD_PRELOAD") == NULL);
 		execvp(server_argv[0], server_argv);
Only in firejail-0.9.60/src/firejail: x11.c.orig
