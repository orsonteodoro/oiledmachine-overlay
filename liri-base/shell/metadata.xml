<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
  <maintainer type="person">
    <!-- For the ebuild only on the oiledmachine-overlay -->
    <email>orsonteodoro@hotmail.com</email>
    <name>Orson Teodoro</name>
  </maintainer>
  <longdescription>

    Build time environment variables

    LIRI_SHELL_DEFAULT_ICON_THEME - Force this icon theme.  The theme must match
    one of the names in /usr/share/icons.

    LIRI_SHELL_PINNED_LAUNCHERS - A space delimited list to appear in the
    launcher taskbar.  Setting this will override the default.  You may set
    this value to REMOVE_ALL to delete all pinned launchers.  You may use
    one of the names in /usr/share/applications without the .desktop portion.

      Examples:

      LIRI_SHELL_PINNED_LAUNCHERS="io.liri.Terminal io.liri.Settings"


    Per user customization

    To reset lannchers:

      gsettings reset io.liri.desktop.panel pinned-launchers

    To add/remove launchers:

      gsettings set io.liri.desktop.panel pinned-launchers ['app1', 'app2', 'app3']
      gsettings set io.liri.desktop.panel pinned-launchers ['app1']
      (The same as /usr/share/applications but without the .desktop suffix.)

    To remove all launchers:

      gsettings set io.liri.desktop.panel pinned-launchers []

    To set icon theme:

      gsettings set io.liri.desktop.interface icon-theme 'Paper'

  </longdescription>
  <upstream>
    <remote-id type="github">lirios/shell</remote-id>
    <bugs-to>https://github.com/lirios/shell/issues</bugs-to>
  </upstream>
  <use>
    <flag name="doc">
      Build documentation
    </flag>
    <flag name="gif">
      Add support for importing gif profile pictures
    </flag>
    <flag name="jpeg">
      Add support for wallpapers in jpeg
    </flag>
    <flag name="qtquick-compiler">
      Optimize using the Qt Quick Compiler.  This will require rebuild of
      this package on Qt updates.
    </flag>
    <flag name="systemd">
      Adds systemd support
    </flag>
    <flag name="rN">
      FOR INTERNAL USE ONLY.  (FORCES REBUILD WHEN BUMPED)
    </flag>
  </use>
</pkgmetadata>
