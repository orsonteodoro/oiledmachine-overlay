--- a/cmake/OpenCVFindLAPACK.cmake.orig	2026-06-23 12:25:46.261472380 -0700
+++ b/cmake/OpenCVFindLAPACK.cmake	2026-06-23 12:27:12.334844550 -0700
@@ -226,10 +226,11 @@ if(WITH_LAPACK)
           LIBRARIES "${LAPACK_LIBRARIES}")
       endif()
       if(NOT HAVE_LAPACK)
+        find_package(CBLAS)
         if(NOT DEFINED CBLAS_INCLUDE_DIR)
           find_path(CBLAS_INCLUDE_DIR "cblas.h")
         endif()
-        if(CBLAS_INCLUDE_DIR AND LAPACKE_INCLUDE_DIR)
+        if(CBLAS_INCLUDE_DIR AND LAPACKE_INCLUDE_DIR AND CBLAS_FOUND)
           if(NOT DEFINED CBLAS_LIBRARY)
             find_library(CBLAS_LIBRARY cblas)
           endif()
@@ -241,7 +242,7 @@ if(WITH_LAPACK)
             CBLAS_H "cblas.h"
             LAPACKE_H "lapacke.h"
             INCLUDE_DIR "${CBLAS_INCLUDE_DIR}" "${LAPACKE_INCLUDE_DIR}"
-            LIBRARIES "${_lapack_generic_libs}")
+            LIBRARIES "${_lapack_generic_libs} ${CBLAS_LIBRARIES}")
         elseif(APPLE)
           ocv_lapack_check(IMPL "LAPACK/Apple"
             CBLAS_H "Accelerate/Accelerate.h"
