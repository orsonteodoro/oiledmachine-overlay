diff '--color=auto' -urp REAL-Video-Enhancer-RVE-2.1.0.orig/REAL-Video-Enhancer.py REAL-Video-Enhancer-RVE-2.1.0/REAL-Video-Enhancer.py
--- REAL-Video-Enhancer-RVE-2.1.0.orig/REAL-Video-Enhancer.py	2024-11-10 15:12:51.000000000 -0800
+++ REAL-Video-Enhancer-RVE-2.1.0/REAL-Video-Enhancer.py	2024-12-14 00:48:19.206999738 -0800
@@ -95,14 +95,14 @@ class MainWindow(QMainWindow, Ui_MainWin
         backendHandler.enableCorrectBackends()
 
         backendHandler.setupBackendDeps()
-        self.backends, self.fullOutput = (
-            backendHandler.recursivlyCheckIfDepsOnFirstInstallToMakeSureUserHasInstalledAtLeastOneBackend(
-                firstIter=True
-            )
-        )
+        #self.backends, self.fullOutput = (
+        #    backendHandler.recursivlyCheckIfDepsOnFirstInstallToMakeSureUserHasInstalledAtLeastOneBackend(
+        #        firstIter=True
+        #    )
+        #)
 
         backendHandler.hideUninstallButtons()
-        backendHandler.showUninstallButton(self.backends)
+        #backendHandler.showUninstallButton(self.backends)
         icon_path = ":/icons/icons/logo-v2.svg"
         self.setWindowIcon(QIcon(icon_path))
         QApplication.setWindowIcon(QIcon(icon_path))
@@ -117,7 +117,7 @@ class MainWindow(QMainWindow, Ui_MainWin
 
         self.QConnect()
         # set up tabs
-        self.backendComboBox.addItems(self.backends)
+        self.backendComboBox.addItems(@BACKENDS_LIST@)
         printOut = (
             "------REAL Video Enhancer------\n"
             + "System Information: \n"
diff '--color=auto' -urp REAL-Video-Enhancer-RVE-2.1.0.orig/build.py REAL-Video-Enhancer-RVE-2.1.0/build.py
--- REAL-Video-Enhancer-RVE-2.1.0.orig/build.py	2024-11-10 15:12:51.000000000 -0800
+++ REAL-Video-Enhancer-RVE-2.1.0/build.py	2024-12-14 00:25:45.127802217 -0800
@@ -127,10 +127,10 @@ python_version = (
 )
 if len(sys.argv) > 1:
     if sys.argv[1] == "--create_venv" or sys.argv[1] == "--build_exe":
-        install_pip()
+        #install_pip()
         create_venv(python_version=python_version)
-        install_pip_in_venv()
-        install_requirements_in_venv()
+        #install_pip_in_venv()
+        #install_requirements_in_venv()
 build_gui()
 build_resources()
 if len(sys.argv) > 1:
diff '--color=auto' -urp REAL-Video-Enhancer-RVE-2.1.0.orig/src/Backendhandler.py REAL-Video-Enhancer-RVE-2.1.0/src/Backendhandler.py
--- REAL-Video-Enhancer-RVE-2.1.0.orig/src/Backendhandler.py	2024-11-10 15:12:51.000000000 -0800
+++ REAL-Video-Enhancer-RVE-2.1.0/src/Backendhandler.py	2024-12-14 00:32:05.869290083 -0800
@@ -61,6 +61,7 @@ class BackendHandler:
             print(e)
 
     def setupBackendDeps(self):
+        return
         # need pop up window
         from .DownloadDeps import DownloadDependencies
 
diff '--color=auto' -urp REAL-Video-Enhancer-RVE-2.1.0.orig/src/DownloadDeps.py REAL-Video-Enhancer-RVE-2.1.0/src/DownloadDeps.py
--- REAL-Video-Enhancer-RVE-2.1.0.orig/src/DownloadDeps.py	2024-11-10 15:12:51.000000000 -0800
+++ REAL-Video-Enhancer-RVE-2.1.0/src/DownloadDeps.py	2024-12-14 00:27:46.047004635 -0800
@@ -18,6 +18,7 @@ from .ui.QTcustom import (
     RegularQTPopup,
 )
 import os
+import sys
 from platform import machine
 import subprocess
 
@@ -62,6 +63,7 @@ class DownloadDependencies:
         createDirectory(os.path.join(currentDirectory(), "bin"))
 
     def downloadBackend(self, tag):
+        return
         """
         Downloads the backend based on the tag of release.
         The tag of release is equal to the tag of the version.
@@ -300,5 +302,6 @@ class DownloadDependencies:
 
 
 if __name__ == "__main__":
+    sys.exit(0)
     downloadDependencies = DownloadDependencies()
     downloadDependencies.downloadPython()
diff '--color=auto' -urp REAL-Video-Enhancer-RVE-2.1.0.orig/src/ModelHandler.py REAL-Video-Enhancer-RVE-2.1.0/src/ModelHandler.py
--- REAL-Video-Enhancer-RVE-2.1.0.orig/src/ModelHandler.py	2024-11-10 15:12:51.000000000 -0800
+++ REAL-Video-Enhancer-RVE-2.1.0/src/ModelHandler.py	2024-12-14 00:25:45.127802217 -0800
@@ -319,6 +319,7 @@ totalModels = (
 
 
 def downloadModelsBasedOnInstalledBackend(installed_backends: list):
+    return
     if NetworkCheckPopup():
         for backend in installed_backends:
             match backend:
diff '--color=auto' -urp REAL-Video-Enhancer-RVE-2.1.0.orig/src/ui/DownloadTab.py REAL-Video-Enhancer-RVE-2.1.0/src/ui/DownloadTab.py
--- REAL-Video-Enhancer-RVE-2.1.0.orig/src/ui/DownloadTab.py	2024-11-10 15:12:51.000000000 -0800
+++ REAL-Video-Enhancer-RVE-2.1.0/src/ui/DownloadTab.py	2024-12-14 00:31:17.149611658 -0800
@@ -57,6 +57,7 @@ class DownloadTab:
         )
 
     def download(self, dep, install: bool = True):
+        return
         """
         Downloads the specified dependency.
         Parameters:
diff '--color=auto' -urp REAL-Video-Enhancer-RVE-2.1.0.orig/src/ui/ProcessTab.py REAL-Video-Enhancer-RVE-2.1.0/src/ui/ProcessTab.py
--- REAL-Video-Enhancer-RVE-2.1.0.orig/src/ui/ProcessTab.py	2024-11-10 15:12:51.000000000 -0800
+++ REAL-Video-Enhancer-RVE-2.1.0/src/ui/ProcessTab.py	2024-12-14 00:25:45.127802217 -0800
@@ -246,7 +246,7 @@ class ProcessTab:
             self.discordRPC.start_discordRPC(
                 method, os.path.basename(self.inputFile), backend
             )
-        if self.modelArch != "custom":  # custom models are not downloaded
+        if False and self.modelArch != "custom":  # custom models are not downloaded
             DownloadModel(
                 modelFile=self.modelFile,
                 downloadModelFile=self.downloadFile,
