diff '--color=auto' -urp lobehub-2.1.34.orig/scripts/serverLauncher/startServer.js lobehub-2.1.34/scripts/serverLauncher/startServer.js
--- lobehub-2.1.34.orig/scripts/serverLauncher/startServer.js	2026-02-28 05:06:21.000000000 -0800
+++ lobehub-2.1.34/scripts/serverLauncher/startServer.js	2026-03-01 19:26:30.447976941 -0800
@@ -9,13 +9,33 @@ const { existsSync } = require('node:fs'
 // Docker: /app/startServer.js -> /app/scripts/_shared/...
 const localPath = path.join(__dirname, '..', '_shared', 'checkDeprecatedAuth.js');
 const dockerPath = '/app/scripts/_shared/checkDeprecatedAuth.js';
-const sharedModulePath = existsSync(localPath) ? localPath : dockerPath;
+
+let sharedModulePath;
+
+// 1. Try relative (local dev: scripts/serverLauncher/ -> scripts/_shared/)
+const localRelative = path.join(__dirname, '..', '_shared', 'checkDeprecatedAuth.js');
+if (existsSync(localRelative)) {
+  sharedModulePath = localRelative;
+}
+// 2. Try Docker-style absolute (/app/...)
+else if (existsSync('/app/scripts/_shared/checkDeprecatedAuth.js')) {
+  sharedModulePath = '/app/scripts/_shared/checkDeprecatedAuth.js';
+}
+// 3. Try Gentoo /opt/lobehub/ install (scripts/_shared lives next to startServer.js)
+else {
+  const optPath = path.join(__dirname, 'scripts', '_shared', 'checkDeprecatedAuth.js');
+  if (existsSync(optPath)) {
+    sharedModulePath = optPath;
+  } else {
+    throw new Error('Cannot locate checkDeprecatedAuth.js in any known path');
+  }
+}
 
 const { checkDeprecatedAuth } = require(sharedModulePath);
 
 // Set file paths
-const DB_MIGRATION_SCRIPT_PATH = '/app/docker.cjs';
-const SERVER_SCRIPT_PATH = '/app/server.js';
+const DB_MIGRATION_SCRIPT_PATH = '/opt/lobehub/docker.cjs';
+const SERVER_SCRIPT_PATH = '/opt/lobehub/server.js';
 const PROXYCHAINS_CONF_PATH = '/etc/proxychains4.conf';
 
 // Function to check if a string is a valid IP address
@@ -116,8 +136,8 @@ ${protocol} ${ip} ${port} ${user} ${pass
 // Function to execute a script with child process spawn
 const runScript = (scriptPath, useProxy = false) => {
   const command = useProxy
-    ? ['/bin/proxychains', '-q', '/bin/node', scriptPath]
-    : ['/bin/node', scriptPath];
+    ? ['/usr/bin/proxychains', '-q', '/usr/lib/node/@NODE_SLOT@/bin/node', scriptPath]
+    : ['/usr/lib/node/@NODE_SLOT@/bin/node', scriptPath];
   return new Promise((resolve, reject) => {
     const process = spawn(command.shift(), command, { stdio: 'inherit' });
     process.on('close', (code) =>
Only in lobehub-2.1.34/scripts/serverLauncher: startServer.js.orig
