# Fixes errors:
# ERROR [Better Auth]: Error Error: Failed query: insert into "verifications" ("created_at", "expires_at", "id", "identifier", "updated_at", "value") values ($1, $2, $3, $4, $5, $6) returning "created_at", "expires_at", "id", "identifier", "updated_at", "value"
# # SERVER_ERROR:  Error: Failed query: insert into "verifications" ("created_at", "expires_at", "id", "identifier", "updated_at", "value") values ($1, $2, $3, $4, $5, $6) returning "created_at", "expires_at", "id", "identifier", "updated_at", "value"
# [cause]: ErrorEvent

# This removes Neon and node-postgres fallbacks for support for postgres.js.

--- a/packages/database/src/core/web-server.ts.orig	2026-03-20 03:39:27.000000000 -0700
+++ b/packages/database/src/core/web-server.ts	2026-03-24 19:54:46.025524709 -0700
@@ -1,7 +1,5 @@
-import { neonConfig, Pool as NeonPool } from '@neondatabase/serverless';
-import { drizzle as neonDrizzle } from 'drizzle-orm/neon-serverless';
-import { drizzle as nodeDrizzle } from 'drizzle-orm/node-postgres';
-import { Pool as NodePool } from 'pg';
+import { drizzle as postgresJsDrizzle } from 'drizzle-orm/postgres-js';
+import postgres from 'postgres';
 import ws from 'ws';
 
 import { serverDBEnv } from '@/config/db';
@@ -23,21 +21,17 @@ If you don't have it, please run \`opens
   }
 
   const connectionString = serverDBEnv.DATABASE_URL;
-
   if (!connectionString) {
     throw new Error(`You are try to use database, but "DATABASE_URL" is not set correctly`);
   }
 
-  if (serverDBEnv.DATABASE_DRIVER === 'node') {
-    const client = new NodePool({ connectionString });
-    return nodeDrizzle(client, { schema });
-  }
-
-  if (process.env.MIGRATION_DB === '1') {
-    // https://github.com/neondatabase/serverless/blob/main/CONFIG.md#websocketconstructor-typeof-websocket--undefined
-    neonConfig.webSocketConstructor = ws;
-  }
+  // === Primary driver: postgres.js (recommended for stability with PG17 + Better Auth) ===
+  const client = postgres(connectionString, {
+    max: 20,                    // connection pool size
+    idle_timeout: 30,
+    connect_timeout: 10,
+    prepare: false,             // critical for avoiding many Drizzle/PG17 issues
+  });
 
-  const client = new NeonPool({ connectionString });
-  return neonDrizzle(client, { schema });
+  return drizzle(client, { schema });
 };
--- a/src/libs/better-auth/define-config.ts.orig	2026-03-20 03:39:27.000000000 -0700
+++ b/src/libs/better-auth/define-config.ts	2026-03-24 19:47:24.038740646 -0700
@@ -30,6 +30,8 @@ import { createSecondaryStorage, getTrus
 import { parseSSOProviders } from '@/libs/better-auth/utils/server';
 import { EmailService } from '@/server/services/email';
 import { UserService } from '@/server/services/user';
+import postgres from "postgres";
+import { drizzle } from "drizzle-orm/postgres-js";
 
 // Configure HTTP proxy for OAuth provider requests in development (e.g., Google token exchange)
 // Node.js native fetch doesn't respect system proxy settings
@@ -93,6 +95,16 @@ interface CustomBetterAuthOptions {
 }
 
 export function defineConfig(customOptions: CustomBetterAuthOptions) {
+  // === NEW: Create postgres.js client for Better Auth ===
+  const postgresClient = postgres(process.env.DATABASE_URL!, {
+    max: 15,                    // The connection pool size.  Increase it to support many users.
+    idle_timeout: 30,
+    connect_timeout: 10,
+    prepare: false,             // helps with PG17 + Drizzle stability
+  });
+
+  const betterAuthDB = drizzle(postgresClient, { schema });
+
   const options = {
     account: {
       accountLinking: {
@@ -182,7 +194,7 @@ export function defineConfig(customOptio
       // Keep a DB-backed fallback when Redis secondary storage entries are unexpectedly missing.
       storeSessionInDatabase: true,
     },
-    database: drizzleAdapter(serverDB, {
+    database: drizzleAdapter(betterAuthDB, {
       provider: 'pg',
       // experimental joins feature needs schema to pass full relation
       schema,
